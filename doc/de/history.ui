!begin_node History & ToDo-Liste

In der ToDo-Liste habe ich alle momentan geplanten Korrekturen und
Erweiterungen aufgefÅhrt. Die Liste ist nach PrioritÑt sortiert.

Ferner habe ich die sog. (!I)History(!i) zusammengetragen, die Liste aller
énderungen und Neuerungen, die es in (!I)ACSpro(!i) gibt bzw. gab. Wie solche
History-Listen normalerweise ist auch diese chronologisch geordnet,
die letzten Neuerungen stehen zuerst in der Liste.

(!B)Hinweis:(!b) Die Versionsnummer der nÑchsten (!I)ACSpro(!i)-Version wurde nun von
V2.40 auf V3.0.0 erhîht, da sich doch zum Teil grundlegende Dinge
geÑndert haben.

!subtoc [all] !depth 1

(!B)Hinweis:(!b) Diese Listen enthalten den Stand vom 2. Februar 2005. öber
das Internet oder Åber den (!link [Hilfe-DÑmon der Maus LL] [Der Hilfe-DÑmon der Maus LL]) erhalten Sie stets
die neueste Version der History und der ToDo-Liste. Die Adresse der
ACSpro-Seiten lautet

!begin_center
     (!url [] [http://acspro.atari.org/])
!end_center

!begin_node ToDo-Liste

Hier habe ich alle bislang geplanten Korrekturen und Erweiterungen in
ihrer bisher bewerteten PrioritÑt (Je wichtiger ein Punkt ist, umso
weiter steht er oben) aufgelistet.

Stand der Liste ist der 2. Februar 2005. Eine i.a. aktuellere Liste
kann Åber den (!link [Hilfe-DÑmon der Maus LL] [Der Hilfe-DÑmon der Maus LL]) abgerufen werden.

!begin_enumerate

  !item (!I)ACSpro(!i) und (!I)ACSpro(!i)-"Erzeugnisse" sollen auch unter Memory
     Protection laufen. (U. Kaiser)

     (!I)ACSpro(!i) sollte hier eigentlich laufen, bislang gab es bei den
     Tests noch keine Probleme unter MultiTOS. Ich bitte hier um
     weitere Tests unter N.AES und Milan-MultiOS (die habe ich beide
     nicht zur VerfÅgung) sowie RÅckmeldung der Ergebnisse - Danke!

  !item Transparente und komfortable UnterstÅtzung bereits im (!I)ACSpro(!i)-
     (!I)GUI-Editor(!i) (soweit mîglich und sinnvoll) und in den Bibliotheken
     von allen modernen Protokolle wie z.B. AV- & XAcc-(!link [Protokoll] [Protokoll])
     (komplett, nicht nur rudimentÑr wie bisher), OLGA, GEMScript,
     ST-Guide, PureC-Hilfe-Protokoll, DHST-Protokoll, SE-(!link [Protokoll] [Protokoll]),
     MenÅ-(!link [Protokoll] [Protokoll]), SSP, etc. (M. ElsÑsser)

     Ein Groûteil der genannten Protokolle wird bereits passiv von
     (!I)ACSpro(!i) unterstÅtzt, d.h. die entsprechenden Nachrichten werden
     empfangen und verarbeitet. Die aktive UnterstÅtzung ist in
     Vorbereitung und wird in einer der Versionen nach der V3.0.0
     realisiert werden.

  !item Als Compiler soll auch der GCC verwendet werden kînnen. (U.
     Kaiser)

     Eine Portierung auf GNU-C ist mit der Version 3.0.0 geplant und
     in Arbeit.

  !item Bei mehr als 256 Farben erhÑlt man entweder schwarz/weiû Icons
     oder PixelmÅll. (H. Achilles)

  !item Wenn (!I)ACSpro(!i)-eigene UserDefs Åber den allgemeinen UserDef-Editor
     mit einer anderen Service-Routine versehen werden, wird diese
     wieder durch die Standard-Routine ersetzt, wenn man den Userdef-
     Objekt-Editor mit 'OK' verlÑût. Eine eigene Service-Routine
     sollte vom Userdef-Objekt-Editor erhalten bleiben oder sogar
     darin eingetragen werden kînnen!

  !item Das Backdropping wird bislang automatisch durch MagiC (Backdrop
     des Fensters nicht angemeldet) bzw. durch (!I)ACSpro(!i) (Backdrop des
     Fensters angemeldet) erledigt und ist nicht beeinfluûbar. (M.
     ElsÑsser)

     In einer der nÑchsten Versionen wird die Struktur Awindow um
     einen Zeiger fÅr das Backdropping erweitert, so daû man hier auch
     eigene Routinen hinterlegen kann.

  !item Die Nachricht WM_MOUSEWHEEL wird empfangen und durch Scrollen des
     Fensters umgesetzt. Hier sollte ein optimiertes Scrollen
     erfolgen, indem ggf. unter der Maus befindliche Objekte gescrollt
     werden sollten.

     Dies wird in einer der nÑchsten Versionen optimiert unterstÅtzt
     werden, d.h. daû bei Aktionen des Mausrades Edit-Felder, (!link [Cycle] [Cycle])-
     Buttons, Popups, etc. gescrollt werden.

  !item Eine UnterstÅtzung der WDIALOG-Druckdialoge, des (MagiC-)Font-
     Selectors und des (MagiC-)Fileselectors jeweils als Fenster-
     Dialog und modaler Dialog sollte (!I)ACSpro(!i) in gewohntem Komfort
     komplett zur VerfÅgung stellen.

     Der (MagiC-)Fileselector sowie der (MagiC-)Font-Selector werden
     in der Version 3.0.0 in vollem Umfang in allen mîglichen (und
     unmîglichen ;-) Varianten unterstÅtzt. Der WDIALOG-Druckdialog
     wird in Version 3.0.0 unterstÅtzt, allerdings kann der
     Druckdialog noch nicht durch eigene ObjektbÑume erweitert werden.
     Das Einbinden eigener ObjektbÑume in den Druckdialog wird in
     einer der nÑchsten Versionen enthalten sein.

  !item Die einzelnen Konfigurationsdateien, die es im Moment gibt, sind
     zum groûen Teil binÑr abgelegt.

     Es ist geplant, alle Konfigurationsdateien durch entsprechende
     Cfg-Strings zu ersetzen. Das Laden von anwendungseigenen Cfg-
     Strings erfolgt bereits automatisch. Die Steuerung Åber die
     Struktur CfgInfo in ACSconfig soll Åber den (!I)GUI-Editor(!i) im
     Hauptmodul geschehen.

     Alle Module sollen ihre Daten aus dieser einen Cfg-Datei laden.
     Die Idee ist hierbei, pro ACS-Modul eine Cfg-Gruppe zu verwenden.
     Daher sind die Gruppennamen, die mit "ACS-" beginnen, fÅr (!I)ACSpro(!i)
     reserviert! Erst wenn die modul-eigene Gruppe nicht vorhanden
     ist, sollte das Modul auf eigene Cfg-Strings zurÅckgreifen und
     diese in die Cfg-Strings der Anwendung Åbertragen. Diese
     öbertragung wird von (!I)ACSpro(!i) unterstÅtzt werden, sobald die Cfg-
     Strings von allen ACS-Modulen verwendet werden.

 !item Die vom (!I)GUI-Editor(!i) generierte Struktur Aconfig enthÑlt noch
     Dummy-Werte fÅr die Komponenten CfgInfo und basename.

     Die hier abgelegten Werte sollen fÅr das Hauptmodul ebenso
     komfortabel im (!I)GUI-Editor(!i) eingegeben werden kînnen, wie die
     anderen Werte der Struktur.

 !item In die Struktur Aconfig sollten (!I)alle(!i) wichtigen Initialisierungs-
     Daten aufgenommen werden - z.B. fehlt die Angabe der
     freigeschalteten (!link [Dead-Keys] [Dead-Keys]), die nun neu hinzugekommen ist. Alle
     diese Intialisierungs-Werte sollen vom (!I)GUI-Editor(!i) aus verwaltet
     werden kînnen.

 !item Nach der linearen unsortierten Liste fehlen noch so nette Dinge
     wie die lineare sortierte Liste, Stack, BinÑrbÑume, AB-BÑume,
     Hashtables, etc.

 !item (!I)ACSpro(!i) und mittels (!I)ACSpro(!i) entwickelte Anwendungen sollen
     problemlos unter MagiC 6 und N.AES mit jeweils proportionalen
     AES-Font laufen. (U. Kaiser)

 !item FÅr Pure-Pascal-Output sollten die Forward-Deklarationen
     ebenfalls ausgegeben werden. (K. Kroll)

 !item Der Icon-Editor sollte Åberarbeitet, am Besten komplett neu
     geschrieben werden. Z.B. sollten bei 256 Farben auch die Icons
     fÅr 16, 4 und 2 Farben bearbeitet werden kînnen...

 !item Die implementierten Cfg-Strings sollten nach Vorhandensein eines
     BinÑrbaumes oder einer Hashtable umgestellt werden, um eine
     wesentlich bessere Performance als mit der bisherigen linearen
     Suche zu erzielen.

 !item Routinen zur Bearbeitung und Auswertung von ObjektbÑumen wie z.B.
     Ausschnitte von BÑumen kopieren, Objekte im Baum zÑhlen etc. (H.
     Achilles)

 !item In (!I)ACSpro(!i) sollten auch SubmenÅs implementiert werden. Derzeit
     sind ja leider nur Subpopups mîglich. (D. Kînig)

 !item Um Eingaben mîglichst sofort plausibilisieren zu kînnen, ist
     einiges an Programmieraufwand notwendig, besonders wenn
     Eingabefelder voneinander abhÑngen. Hier soll (!I)ACSpro(!i) einen
     Mechanismus bieten, der ohne groûen Aufwand auch komplexe
     Plausibilisierungen ermîglicht.

 !item Bessere UnterstÅtzung der Mehrsprachigkeit! Bisher muû jede ACS-
     Datei im (!I)ACSpro(!i)-(!I)GUI-Editor(!i) geîffnet und auf die neue Sprache
     umgestellt werden. Nach einem Generieren der Compiler-Sourcen muû
     erneut compiliert werden. (M. ElsÑsser)

     Hier wird (!I)ACSpro(!i) in Zukunft alle Sprach-Varianten exportieren.
     Durch Compiler-Macros wird dann die gewÅnschte eingebunden, man
     muû z.B. in Pure-C nur das passende Sprach-Makro definieren. Wie
     dies allerdings in Pure Pascal funktionieren wird, muû ich erst
     noch "erforschen"... ;-/

     Als weitere Variante kînnen die Strings natÅrlich aus der
     Anwendung ausgelagert werden. Wie dann allerdings aus vielen
     Modulen bestehende Anwendungen zu einer "Text-Ressource" kommen,
     ist leider noch nicht ganz klar. Diese letzte Methode wird
     favorisiert und in einer der Versionen nach V3.0.0 realisiert
     werden, da hierdurch auch unabhÑngige öbersetzer Anwendungen in
     andere Sprachen Åbersetzen kînnen.

 !item Beim Kopieren von Objekten im (!I)GUI-Editor(!i) (Shift-Taste beim
     Draggen gedrÅckt) werden abhÑngige Objekte (meist Strings) nur
     referenziert. Durch zusÑtzliches DrÅcken der Control-Taste werden
     diese dann auch kopiert. Diese momentane Standard-Einstellung der
     ausschlieûlichen Referenzierung beim Kopieren von Objekten im
     (!I)GUI-Editor(!i) sollte umgekehrt durch Control aktivierbar sein, da
     die Referenzierung eher als Sonderfall von Nutzen ist.

     Im Normalfall zieht man ein z.B. Button-Objekt aus dem Teile-
     Fenste und stellt die Grîûe, Farbe etc. ein. Wenn man nun z.B. 10
     von diesen Objekten benîtigt, konnte man frÅher die Anzahl von
     Kopien anlegen und danach die unterschiedlichen Boxtexte
     eingeben. Nun jedoch muû dieser Arbeitsgang fÅr jedes einzelne
     Objekt erneut ausgefÅhrt werden. Das ist ein erheblicher
     Mehraufwand und spart auch nicht ein Byte an redundantem Code.
     Vîllig identische Objekte sind hingegen nur selten gefragt. (D.
     Kînig)

 !item Moderne AES kînnen im MenÅ eine ansprechende Trennlinie zeichnen.
     Diese sollte gegenÅber der (!I)ACSpro(!i)-Lîsung eines UserDef vorgezogen
     werden. (U. Kaiser)

     In (!I)ACSpro(!i) 3.0.0 ist nun das Aussehen der MenÅs an die unter MagiC
     bei proportionalem Systemzeichensatz dargestellten 3D-MenÅs
     angepaût worden. Der 3D-Look der MenÅs ist allerdings unabhÑngig
     vom proportionalen Systemzeichensatz. Der 3D-Look der MenÅs kann
     ebenso abgeschaltet werden.

 !item Im Sinne eines einheitlichen und damit wirklich
     benutzerfreundlichen GUI sollte sich der Look von (!I)ACSpro(!i)-
     Anwendungen unbedingt an den gÑngigen Look anpassen. (U. Kaiser)

     Dies sollte im Groûen und Ganzen fÅr magiC bereits realisiert
     sein. Noch fehlende Punkte mîchte ich natÅrlich noch anpassen.
     (!I)ACSpro(!i)-Anwendungen sollten von der Optik her wie andere moderne
     Anwendungen auch erscheinen - dies ist auch unter Ñlteren TOS-
     Versionen so.

     Allerdings mîchte ich optional die ACS-Userdefs in entsprechende
     GEM-Objekte wandeln, falls diese zur VerfÅgung stehen, z.B.
     sollten die Checkboxen in MagiC-Checkboxen umgewandelt werden.
     Allerdings ist hier zunÑchst aufgrund der ACS-FunktionalitÑt Åber
     Service-Routinen noch konzeptionelle Arbeit notwendig.

 !item (!I)ACSpro(!i) soll entweder die MT_AES.LIB oder ein eigenes
     (optimiertes?) Multithreading-festes Binding verwenden. Ferner
     sind komfortable Mîglichkeiten fÅr Threads nÅtzlich. (H.
     Achilles)

     (!I)ACSpro(!i) enthÑlt nun ein eigenes TOS- und GEM-Binding. Das
     verwendete GEM-Binding ist allerdings noch nicht die fÅr (!I)ACSpro(!i)
     optimierte, multithreadingfeste Version.

 !item Das (!link [Cycle] [Cycle])-Objekt liefert leider bei Aufruf von identischen Text-
     EintrÑgen mittels AUO_CYGETINDEX immer nur den Index des ersten
     vorkommenden Eintrags. Das ist nicht WYSIWYG. Wenn ich z.B. 10
     mal "Test|" als String Åbergebe und auf den 10. Eintrag klicke,
     darf nicht der Index 0 zurÅckgegeben werden. Hier mÅûte der
     Such-Algorithmus die Position im Objektbaum ermitteln und nicht
     die erste String-Position im Popupstring. éhnliches Fehlverhalten
     ist bei Subpopups zu bemÑngeln. Vorerst muû man also darauf
     achten, daû keine identischen Strings in den Popups vorkommen.
     Eine lÑstige EinschrÑnkung. (D. Kînig)

 !item Die Funktion Ame_strpopup îffnet bei öbergabe des Pointers (!I)chk(!i)
     das Popup mit der Maus Åber diesem Popup-Objekt. Leider trifft
     das nicht zu, wenn das gewÅnschte Objekt in einem Subpopup
     vorkommt. Die Routine kînnte so umprogrammiert werden, daû in
     diesem Fall die Subpopups automatisch aufgeklappt werden und die
     Maus dann auch Åber diesem gewÅnschten Objekt steht. Das sieht
     erheblich professioneller aus und man muû sich nicht jedesmal mit
     der Maus zu diesem "Ausgangspunkt" hindurchhangeln, wenn man
     innerhalb einer "Gruppe" von Sub(subsub)popups etwas auswÑhlen
     will. (D. Kînig)
!end_enumerate

!end_node


!begin_node énderungen Version V2.3.3 auf V3.0.0

Hier sind die énderungen und Erweiterungen von V2.3.3 zur - noch nicht
verîffentlichten - V3.0.0 in umgekehrter chronologischer Reihenfolge
aufgelistet.

(!B)Hinweis:(!b) énderungen an den C-Bibliotheken betreffen stets auch die
(!link [Pascal-Bibliothek] [Pascal-Bibliothek]). Von Bugfixes und Erweiterungen in den Bibliotheken
hat stets auch der (!I)GUI-Editor(!i) profitiert, da dieser ja mittels (!I)ACSpro(!i)
entwickelt wurde.

Stand der Liste ist der 2. Februar 2005. Eine i.a. aktuellere Liste
kann Åber den (!link [Hilfe-DÑmon der Maus LL] [Der Hilfe-DÑmon der Maus LL]) abgerufen werden.

 02.02.2005 (!I)GUI-Editor(!i)

         !begin_itemize
         !item Der (!I)GUI-Editor(!i) exportiert beim Generieren der Sourcen nun
           alle vier enthaltenen Sprachen. öber die Macros ACS_LANG1
           bis ACS_LANG4 wird beim Compilieren entschieden, welche
           Sprachvariante eingebunden werden soll. Falls keiner der
           Macros definiert ist, wird das der im (!I)GUI-Editor(!i)
           eingetragenen Sprache der ACS-Datei verwendet.

           Es ist damit sehr leicht, ein ACS-Programm fÅr andere
           Sprachen zu compilieren. Die zu verwendende Sprache kann
           entweder in der Projektdatei (Pure-)C, in einer Include-
           Datei oder global z.B. in der Pure-C-IDE eingestellt
           werden. FÅr (!I)ACSpro(!i) selbst - sowohl fÅr den (!I)GUI-Editor(!i) als
           auch fÅr die Bibliotheken - wird die Sprach-Variante in
           Pure-C unter Options\Compiler eingestellt.
         !end_itemize

 31.01.2005 (!link [ACS.LIB] [Pure-C-Bibliothek]), (!I)GUI-Editor(!i)

         !begin_itemize
         !item Die Copyright-Informationen wurden angepaût.

         !item Integration der KEYTAB-Bibliothek zu KEYTAB 12.
         !end_itemize

 04.10.2004 ACSMOD.LIB, ACSMOD01.LIB, ACS_LOAD.O

         !begin_itemize
         !item Wegen eines Fehlers in der Modul-Schnittstelle konnten
           Module, die mit der vorherigen Beta-Version von (!I)ACSpro(!i)
           erstellt wurden, nicht mehr geladen werden. Das Problem
           wurde behoben.
         !end_itemize

 02.06.2004 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Wenn in den (!link [Cfg-Strings] [Die Konfigurations-Strings]) mind. eine Parametergruppe mit
           Namen, jedoch keine namenlose Default-Gruppe vorhanden war,
           so wurden danach angelegte Parameter der Default-Gruppe
           mitten in den anderen Gruppen gesichert und beim Laden
           natÅrlich einer anderen Gruppe zugeordnet. Nun wird die
           namenlose Default-Gruppe stets zuerst gesichert.
         !end_itemize

 06.04.2004 (!link [ACS.LIB] [Pure-C-Bibliothek]), (!link [ACSPLUS.LIB] [C-Erweiterungs-Bibliothek]), (!I)GUI-Editor(!i)

         !begin_itemize
         !item Bei den Konstanten der NKCC-Routinen wurden fÅr bisher
           reservierte Werte Konstanten vergeben. Es sind dies

            !begin_xlist [NK_M_PGUP ($05)]
            !item [NK_M_PGUP ($05)]  Auf dem Apple Macintosh unter (!link [MagiCMac] [Mag!X]):
                             eine Seite nach oben (Page Up)

            !item [NK_M_PGDN ($06)]  Auf dem Apple Macintosh unter (!link [MagiCMac] [Mag!X]):
                             eine Seite nach unten (Page Down)

            !item [NK_M_END ($07])   Auf dem Apple Macintosh unter (!link [MagiCMac] [Mag!X]):
                             zum Ende springen (End)

            !item [NK_M_F11 ($1A)]   Auf dem Apple Macintosh unter (!link [MagiCMac] [Mag!X]):
                             Funktionstaste F11

            !item [NK_M_F12 ($1C)]   Auf dem Apple Macintosh unter (!link [MagiCMac] [Mag!X]):
                             Funktionstaste F12

            !item [NK_M_F14 ($1D)]   Auf dem Apple Macintosh unter (!link [MagiCMac] [Mag!X]):
                             Funktionstaste F14
            !end_xlist

         !item Die Reaktion unter (!I)ACSpro(!i) ist standardmÑûig die folgende:

            !begin_xlist [NK_M_PGUP ($05)]
            !item [NK_M_PGUP ($05)]  Wenn keine Shift-Taste gedrÅckt wird,
                             wird eine Seite nach oben gescrollt. So
                             war es bisher bei Shift-Cursor-Up.

                             Wenn Shift gedrÅckt wird, wird anstelle
                             einer Seite eine Zeile nach oben
                             gescrollt, wie es bisher bei Cursor-Up
                             der Fall war.

            !item [NK_M_PGDN ($06)]  Wenn keine Shift-Taste gedrÅckt wird,
                             wird eine Seite nach unten gescrollt. So
                             war es bisher bei Shift-Cursor-Down.

                             Wenn Shift gedrÅckt wird, wird anstelle
                             einer Seite eine Zeile nach unten
                             gescrollt, wie es bisher bei Cursor-Down
                             der Fall war.

            !item [NK_M_END ($07)]   Wenn keine Shift-Taste gedrÅckt ist, wird
                             an das Ende gesprungen, wie es bisher
                             auch bei Shift-Clr/Home der Fall war.

                             Wenn Shift gedrÅckt wird, wird an den
                             Anfang wie bei Clr/Home gesprungen.
            !end_xlist

           Das bisherige Verhalten der Tasten Shift-Cursor-Up/Down
           bzw. Shift-Clr/Home bleibt auf jeden Fall bestehen.
         !end_itemize

 06.04.2004 ACSMOD.LIB, ACSMOD01.LIB, ACS_START.O

         !begin_itemize
         !item Im Startup-Code des Moduls ist nun auch das Symbol _globl
           enthalten. Module, die GEM-Funktionen verwenden und z.B.
           eigene Bindings implementieren und daher nicht auf die Åber
           die Modul-Schnittstelle zur VerfÅgung gestellten Funktionen
           zugreifen, kînnen mit der Bibliothek PCGEM.LIB gelinkt
           werden. Diese sieht im KompatibilitÑtsmodus eine globale
           Variable _globl vor, die das global-Array enthÑlt. Im
           Startup-Code ist nun diese Variable enthalten, sie wird von
           (!I)ACSpro(!i) aus initialisiert.

           Leider mÅssen damit alle Module, die mit einer der
           bisherigen Beta-Versionen 3.0.0û erstellt wurden, erneut
           gelinkt werden. Ich denke jedoch, das sollte machbar sein.
         !end_itemize

 29.03.2004 (!link [ACS.LIB] [Pure-C-Bibliothek]), ACSTOOLS.LIB, ACSMOD.LIB, ACSMOD01.LIB

         !begin_itemize
         !item Die neuen Funktionen von KEYTAB und der KEYTAB-Bibliothek
           wurden in (!I)ACSpro(!i) integriert. Die Funktionen stehen auch
           Åber die Modul-Schnittstelle fÅr Module zur VerfÅgung.
         !end_itemize

 01.02.2004 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Neue (!link [Cookies] [Cookie Jar]) aus der MiNT-Mailingliste in ACSCOOK.H
           eingefÅgt.
         !end_itemize

 19.01.2004 (!link [ACS.LIB] [Pure-C-Bibliothek]), ACSMOD.LIB, ACSMOD01.LIB

         !begin_itemize
         !item Die unsortierte lineare Liste ULinListe wurde in ULinList
           umbenannt. Der bisherige Name ULinListe wird per Makro in
           den neuen Namen gewandelt.

         !item Zur unsortierten linearen Liste sind die "verwandten"
           Objekte Stack (Stapel) und Queue (Warteschlange) hinzu
           gekommen.

         !item Die create- und delete-Funktionen der beiden neuen Objekte
           Queue und Stack steht auch Åber die Modul-Schnittstelle zur
           VerfÅgung
         !end_itemize

 01.01.2004 (!link [ACS.LIB] [Pure-C-Bibliothek]), (!I)GUI-Editor(!i)

         !begin_itemize
         !item Die Copyright-Informationen wurden angepaût.
         !end_itemize

 10.11.2003 (!I)GUI-Editor(!i)

         !begin_itemize
         !item Die Texte des (!I)GUI-Editor(!i)s wurden ins Englische Åbersetzt,
           so daû nun auch eine Beta-Version der englischen Version
           bereitsteht.
         !end_itemize

 30.07.2003 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Die Bibliotheken PCGEM.LIB (GEM-Binding) und ACSTOS.LIB
           (TOS-Binding) sind nicht mehr in (!link [ACS.LIB] [Pure-C-Bibliothek]) enthalten. Damit
           kînnen die Bindings jederzeit gegen aktuellere Versionen
           ausgetausht werden. Alle Anwendungen mÅssen daher die
           PCTOS.LIB und die PCGEM.LIB linken, die ab jetzt (!I)ACSpro(!i)
           beiliegen.

         !item (!I)ACSpro(!i) wurde nun komplett auf die TOS-Bibliothek PCTOS.LIB
           umgestellt. Bei den TOS-Bindings bringt die optimierte
           Version nicht wirklich viel, dafÅr wird die KompatibilitÑt
           mit div. Dokumentationen aufgegeben. Nachdem es nur um ein
           paar wenige Funktionen geht, sollte diese Umstellung kein
           Problem bereiten.
         !end_itemize

 29.12.2002 (!link [ACS.LIB] [Pure-C-Bibliothek]), ACSMOD.LIB, ACSMOD01.LIB

         !begin_itemize
         !item Die neue Funktion Avdi_getRGB liefert fÅr die 256 Farben
           der NVDI-Palette die passenden RGB-Werte zurÅck. Damit
           stehen die Farb-Informationen fÅr Objekte, die "nur" 256
           Farben kennen, auch unter Hi- oder Truecolor-Auflîsungen
           zur VerfÅgung fÅr ein korrektes Zeichnen. Die Funktion
           steht auch Åber die Modul-Schnittstelle zur VerfÅgung.

         !item Die Funktion Af_2path liefert anstelle des Root-Pfades "\"
           den Defaultpfad des Defaultlaufwerkes - also Dgetpath(dest,
           0) -, falls kein Pfad im Parameter erkannt wird. Erstens
           entspricht dieses Verhalten dem der Funktion Af_2drv. Und
           zweitens ist dieses Verhalten bei relativen Pfadangaben
           auch gewÅnscht.
         !end_itemize

 22.11.2002 (!link [ACS.LIB] [Pure-C-Bibliothek]), ACSMOD.LIB, ACSMOD01.LIB

         !begin_itemize
         !item Die neue Funktion Aob_count zÑhlt die Anzahl der OBJECT-
           Strukturen eines Objektbaumes. Das Ende wird - wie bei GEM
           Åblich - durch das Flag LASTOB in ob_flags signalisiert.
           Die AOBJECT-Strukturen werden beim ZÑhlen je nach
           Åbergebenem Flag Åbersprungen oder mitgezÑhlt. Die Funktion
           steht auch Åber die Modul-Schnittstelle zur VerfÅgung.
         !end_itemize

 13.11.2002 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Beim Versand von Daten per D&D an andere Anwendungen war
           noch ein dicker Bug: die LÑnge der verschickten Daten wurde
           bei Strings (ACS-DD-Type AT_STRING, AT_TEXT und AT_FILE)
           falsch verschickt, so daû der EmpfÑnger nur einen Teil der
           Daten verwendet hat.

         !item Das Auswahl-Popup, in dem die Ziel-Anwendung ausgesucht
           werden muû, erscheint nun nur noch, wenn der EigentÅmer des
           Ziel-Fensters nicht per WF_OWNER ermittelt werden kann
           (WF_OWNER gibt es nicht bzw. liefert Fehler).

         !item Von anderen Anwendungen empfangene Strings, die Åber die
           (!link [Drag-Routine] [Click-Routine]) eingespielt werden, werden nun korrekt als
           AT_STRING oder AT_TEXT deklariert, jenachdem ob eines der
           Zeichen '\n' (CR) oder '\r' (LF) enthalten ist oder nicht.

         !item Per XAcc-(!link [Protokoll] [Protokoll]) empfangene Strings werden nun nicht nur
           als AT_STRING, sondern auch als AT_FILE deklariert, falls
           es sich bei dem String gem. Af_length() um den Namen einer
           existierenden Datei bzw. eines existierenden Ordners
           handelt. Die Unterscheidung in AT_TEXT oder nicht hat
           allerdings Vorrang.

         !item Korrektur beim Verarbeiten einer D&D-Message: das D&D-
           (!link [Protokoll] [Protokoll]) sieht einen Header vor, der mind. die folgenden
           Angaben enthÑlt: Header-LÑnge (2 Byte), Datentyp (4 Byte),
           DatenlÑnge (4 Byte). (!I)ACSpro(!i) meldete bei einer HeaderlÑnge
           von weniger als 10 Byte dem Absender der Daten einen
           Fehler. Bei Tests hat sich herausgestellt, daû MagicDesk
           eine LÑnge von 9 eintrÑgt. Die PrÅfung wurde daher auf mind
           8 Zeichen geÑndert. Anscheinend sind sich die "Gelehrten"
           nicht einig, ob die LÑngenangabe zum Header gehîrt oder
           nicht! (!I)ACSpro(!i) geht nun davon aus, daû die zwei Byte
           HeaderlÑnge (!I)nicht(!i) zum Header gehîren, sondern die
           HeaderlÑnge angibt, wie viele Byte (!I)danach(!i) kommen.
         !end_itemize

 21.09.2002 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Die Bearbeitung der GEMScript-Meldung GS_ACK wurde
           korrigiert.

         !item Durch interne Optimierungen bei den diversen unterstÅtzten
           Protokollen konnte etwas benîtigter Speicher eingespart
           werden.
         !end_itemize

 13.09.2002 (!link [ACS.LIB] [Pure-C-Bibliothek]), ACSMOD.LIB, ACSMOD01.LIB

         !begin_itemize
         !item Die aktuelle PCGEM.LIB wurde eingebunden. Die neuen
           Funktionen (bis NVDI 5) stehen auch in der Modul-
           Schnittstelle zur VerfÅgung.

         !item Die Modul-Schnittstelle fÅr vsf_updat war nicht in Ordnung,
           dadurch gab es einige Verschiebungen, so daû im
           Zweifelsfall falsche VDI-Funktionen aufgerufen wurden. Dies
           wurde nun behoben!

         !item Die aktuelle KEYTAB-Bibliothek wurde integriert, die neuen
           Funktionen stehen auch in der Modulschnittstelle fÅr Module
           bereit.
         !end_itemize

 31.08.2002 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Die Funktion appl_xgetinfo prÅft nun auch, ob WINX
           vorhanden ist, da auch dann appl_getinfo aufgerufen werden
           kann.
         !end_itemize

 06.08.2002 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Die Nachricht COLORS_CHANGED (s. Dokumentation zu NVDI 5)
           wird verarbeitet und ein komplettes Neuzeichnen des
           Bildschirms ausgelîst.
         !end_itemize

 22.07.2002 (!link [ACS.LIB] [Pure-C-Bibliothek]), ACSTOOLS.LIB, ACSMOD.LIB, ACSMOD01.LIB

         !begin_itemize
         !item Die neuen Funktionen von KEYTAB und der KEYTAB-Bibliothek
           wurden in (!I)ACSpro(!i) integriert. Die Funktionen stehen auch
           Åber die Modul-Schnittstelle fÅr Module zur VerfÅgung.

         !item Die neue Funktionen Ax_recycle, Ax_setRecycleSize und
           Ax_getRecycleStat stehen auch Åber die Modul-Schnittstelle
           fÅr Module zur VerfÅgung.
         !end_itemize

 04.07.2002 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Die neue Funktion Ax_getRecycleStat() liefert die aktuelle
           Statistik der Speicher-Verwaltung der freien Listen. Mit
           den hier gelieferten Daten kann die Konfiguration der
           Speicher-Verwaltung in der ACSinit0-Routine optimiert
           werden.

         !item Die neue Funktion Ax_setRecycleSize() verwaltet die Listen
           der freien Speicherblîcke:

              !begin_itemize
              !item Wenn eine unbekannte Grîûe Åbergeben wird, wird eine
                neue Liste angelegt.

              !item Eine (!I)ACSpro(!i) bekannte Grîûe wird auf den neuen
                Maximalwert gesetzt (falls die Liste verkleinert wird,
                werden alle Blîcke, die nun zu viel gepuffert werden,
                frei gegeben).

              !item Falls fÅr eine bekannte Blockgrîûe die Anzahlder zu
                puffernden Blîcke auf 0 gesetzt wird, so werden
                Speicherblîcke dieser Grîûe nicht mehr gepuffert und
                die Liste dazu gelîscht.
              !end_itemize

         !item Die neue Funktion Ax_recycle() fÅgt einen Speicherblock
           einer Liste freier Blîcke an, sofern die Åbergebene Grîûe
           des Blockes von (!I)ACSpro(!i) in einer Liste verwaltet wird. Falls
           die fÅr die Blockgrîûe eingestellte Maximalzahl von
           aufzubewahrenden Blîcken erreicht ist, wird der Block
           ebenso wie bei einer nicht verwaltete Blockgrîûe direkt per
           Ax_free frei gegeben.

         !item Das Speicher-Management um die Funktionen Ax_malloc() und
           Ax_free() wurde um Verwaltung von leeren Listen erweitert.

           (!I)Wie funktioniert das?(!i)
           Nun, es gibt eine interne Funktion, die (!I)ACSpro(!i) zur Freigabe
           der eigenen Strukturen wie z.B. Awindow, TEDINFO, BITBLK,
           AUSERBLK, ULinList oder intern gekapselter Strukturen
           verwendet. Diese Funktion prÅft, ob es fÅr die
           entsprechende Blockgrîûe eine Liste fÅr freie Blîcke gibt.
           Falls dies der Fall ist, wird der Block nicht freigegeben,
           sondern in dieser Liste aufbewahrt - allerdings wird fÅr
           jede dieser Listen eine Obergrenze beachtet, ab der der
           Speicher - wie bisher auch - per Ax_free freigegeben wird.
           Falls danach ein Block dieser Grîûe angefordert wird, wird
           er direkt aus dieser Liste genommen und braucht nicht
           erneut alloziert werden.

           Da die Speicherverwaltung der (!link [Pure-C-Bibliothek] [Pure-C-Bibliothek]) die Blîcke
           nicht sofort an das Betriebssystem zurÅckgibt, sondern
           grîûere Blîcke anfordert, diese verwaltet und auf
           Anforderung an die Anwendung gibt, ergibt sich eigentlich
           kein nennenswerter Mehrverbrauch an Speicher. Da (!I)ACSpro(!i)
           jedoch die Grîûe der eigenen Strukturen kennt, kann (!I)ACSpro(!i)
           hier viel effizienter die Verwaltung Åbernehmen. Besonders
           bei den Strukturen AUSERBLK macht sich dies positiv
           bemerkbar, da diese bei allen ACS-eigenen Objekten
           verwendet werden.
         !end_itemize

 03.07.2002 (!I)GUI-Editor(!i)

         !begin_itemize
         !item Der (!I)GUI-Editor(!i) verwendet nun Version 1.4 des Startup-Codes
           von Stefan Kuhr @OS. Im ReadMe dazu steht:

              !begin_itemize
              !item Die Zeiger auf die Environmentvariablen und
                Kommandozeilenargumente verringern die Stack-Grîûe nun
                nur noch bei Accessories oder zu kleiner TPA. Ein
                minimaler Stack bleibt immer frei.

              !item Wenn der Heap zu klein ist - TPA ist schon geschrumpft
                und der Stack ist kleiner als ein Mindestwert -,
                beendet der Startup-Code das Programm mit dem Exit-
                Code 0x4150.

              !item Auch bei viel zu vielen Environmentvariablen oder
                Argumenten wird niemals Åber das TPA-Ende
                hinausgeschrieben. Gleich zu Beginn wird der maximale
                Heap berechnet: MaxHeapSize = TPA - BSSEnde -
                MinimalStack. Mit dem Schreiben der argv- und envp-
                Zeiger wird der Heap gefÅllt und bei Erreichen der
                maximalen Heap-Grîûe wird die Auswertung abgebrochen.
                Auf das reele Heap-Ende wird die Stack-Grîûe addiert
                und so das gewÅnschte Ende der TPA bestimmt. Falls
                mîglich wird dann noch die TPA geschrumpft.

              !item Die Basepagekommandozeile wird nun ins BSS kopiert und
                nicht mehr direkt in der Basepage geparst, da ein
                Fsfirst() ohne vorheriges Fsetdta() die Kommandozeile
                zerstîrt.

              !item Bei einem zu hohen Wert fÅr die
                BasepagekommadozeilenlÑnge wurde an eine falsche
                Position gesprungen und dadurch ein RTS ausgefÅhrt,
                ohne das eine gÅltige RÅcksprungadresse auf dem Stack
                war.

              !item Durch einen falschen Vergleichen mit '0' wurden leere
                ARGV-Argumente ab dem zehnten nicht mehr geleert.

              !item Den kleinen Startup-Code habe ich nochmal Åberarbeitet
                und ein wenig optimiert. Unter anderem habe ich
                argv[0], argv[1] und envp bereits initialisiert ins
                Datensegment gelegt.
              !end_itemize

           Ein dickes Danke an Stefan Kuhr @ OS fÅr seinen Startup-
           Code und dessen Pflege. Besonders natÅrlich ein dickes
           Danke, daû ich ihn (!I)ACSpro(!i) beilegen darf!
         !end_itemize

 24.06.2002 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Die Funktion Af_fileselect() hat einen weiteren Parameter
           bekommen. Nach dem Extensions-(!link [Pattern] [Pattern]) wird nun die
           Sortierung des File-Selectors angegeben. Diese Sortierung
           wird nur fÅr die FSLX-Funktionen verwendet. Falls die
           FSLX-Funktionen nicht verfÅgbar sind und daher auf
           Af_select() bzw. Af_first_fsel() und Af_next_fsel()
           zurÅckgegriffen werden muû, so wird diese Sortierung (!I)nicht(!i)
           berÅcksichtigt.
         !end_itemize

 23.06.2002 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Die Funktion Ash_font liefert nun bei Verwendung des
           modalen Font-Selektors in der Åbergebenen Struktur
           A_FontSel die Ergebnisse zurÅck.

         !item In der Funktion Af_fileselect wurde die Verwendung der
           Extensions-Patterns nochmals Åberarbeitet und korrigiert.
           Nun sollte alles korrekt laufen.
         !end_itemize

 05.06.2002 (!I)GUI-Editor(!i)

         !begin_itemize
         !item Der (!I)GUI-Editor(!i) verwendet nun Version 1.3 des Startup-Codes
           von Stefan Kuhr @ OS. Im ReadMe dazu steht:

              !begin_itemize
              !item Der Parser hat nun eine C-Schnittstelle, so daû er nun
                auch fÅr z.B. AV_START genutzt werden kann. Die
                Schnittstelle ist wie folgt definiert:

!begin_sourcecode
unsigned long parse_cmdline(
         char *cmdline,
         char *cmdarg, char **argv,
         unsigned long max_argc );
!end_sourcecode

                 !begin_xlist [max_argc]
                 !item [cmdline]  ist ein Zeiger auf eine Kommandozeile, die
                          durch ein Nullbyte abgeschlossen ist, und
                          kein LÑngenbyte besitzt.

                 !item [cmdarg]   verweist auf einen Speicher, in dem die
                          geparsten Argumente abgelegt werden sollen.
                          Ist dieser Zeiger NULL, wird cmdline als
                          Zielspeicher verwendet.

                 !item [argv]     bestimmt die Adresse, an der bis zu max_argc
                          Zeiger auf die einzelnen Argumente der
                          Kommandozeile hintereinander abgelegt
                          werden. Sofern das Feld groû genug ist, ist
                          argv[Ergebnis]==NULL. Es ist aber auch
                          mîglich, einen Nullpointer an argv zu
                          Åbergeben.

                 !item [max_argc] beschreibt die Grîûe des Feldes argv.
                 !end_xlist

                Ergebnis ist die Anzahl der Argumente in der
                Kommandozeile.

                Die ParameterÅbergabe erfolgt in Pure-C-Manier, es
                werden dazu die Register D0, A0, A1 und ein Argument
                auf dem Stack genutzt. Alle anderen Register bleiben
                unverÑndert.

              !item Bei einer defekten, erweiterten ARGV-öbergabe, konnte
                es zu Speicherschreibern kommen. Ich habe nun alle
                unsicheren Stellen abgedichtet.

              !item Ein ARGV=NULL:0 wurde nicht ausgewertet. (Naja, macht
                eh keinen groûen Sinn den Programmnamen zu lîschen;
                aber nun geht auch das.)

              !item Die Register werden nun als unsigned betrachtet, dass
                heiût es kînnen theoretisch Åber vier Milliarden
                Argumente und Environmentvariablen gehandhabt werden.
                Auch argc wird als unsigned long an main() Åbergeben.

              !item Die Baspagekommandozeile wird nun nicht mehr auf den
                Heap kopiert (was dem Stack 128 Byte kostete), sondern
                direkt in der Basepage geparst.

              !item Nach wie vor wird der Speicher fÅr die Felder envp und
                argv vom Stack abgeknappst. Das sollte bei der Wahl
                der Stackgrîûe bedacht werden. Vorsichtige Naturen
                kînnen die unterste Stackadresse _StkLim entnehmen.

              !item Selbst bei korrupter ArgumentÅbergabe sind die
                Parameter an main() konsistent.

              !item Der kleine Startup-Code ist noch um eine Kleinigkeit
                geschrumpft.
              !end_itemize

           Ein dickes Danke an Stefan Kuhr @ OS fÅr seinen Startup-
           Code und dessen Pflege. Besonders natÅrlich ein dickes
           Danke, daû ich ihn (!I)ACSpro(!i) beilegen darf!
         !end_itemize

 30.05.2002 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Die Funktion Awi_obfind() stÅrzte mit einem Bus-Error ab,
           wenn als **tree der Zeiger NULL bzw. NIL Åbergeben wurde.

         !item Die Funktion nkc_kstate() brachte ebenfalls hin und wieder
           einen Bus-Error zustande.
         !end_itemize

 26.05.2002 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Bei den Funktionen A_dialog und A_dialog2 wird nun wie bei
           Awi_dialog das HauptmenÅ deaktiviert.

         !item Falls bei A_dialog oder A_dialog2 ein Dialog ohne Edit-
           Felder verwendet wurde, gab es einen Åblen Absturz, der nun
           behoben sein sollte.

         !item Aktuelle PCGEM.LIB mit Fehlerkorrektur fÅr fsel_exinput
           sowie UnterstÅtzung der MausrÑder gemÑû TORG 105 (siehe
           http://www.tosgroup.org - wie z.B. von MWheel von Armin
           Diedering verwendet - wird verwendet.
         !end_itemize

 23.05.2002 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Die HOT-Close-Box wird ggf. abgeschaltet, wenn das System
           sie gemÑû appl_getinfo nicht kennt.
         !end_itemize

 22.05.2002 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Probleme bei ParameterÅbergabe via VA_START und D&D
           korrigiert.
         !end_itemize

 06.05.2002 (!link [ACSPLUS.LIB] [C-Erweiterungs-Bibliothek])

         !begin_itemize
         !item Der DESKTOP hatte noch einen kleinen Bug: beim Doppelklick
           auf das NEU-Icon wurde ein nicht initialisierter String
           anstelle des Wertes NULL Åbergeben.
         !end_itemize

 04.04.2002 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Bei Verwendung des "alten MenÅ-Layoutes" gab es einen
           Absturz, der nun behoben sein sollte.
         !end_itemize

 01.04.2002 ACSMOD.LIB, ACSMOD01.LIB

         !begin_itemize
         !item Die neue Funktion Ash_getOSHeader steht auch Åber die
           Modul-Schnittstelle fÅr Module zur VerfÅgung.

         !item Die KompatibilitÑts-Funktionen Aob_create und Aus_create
           fÅr Module der ACS-Versionen 2.30 bis 2.3.4 waren
           fehlerhaft in die Schnittstelle eingebunden. Module, die
           nur Awi_create verwendet haben, haben funktioniert. Sobald
           allerdings Aob_create oder Aus_create verwendet wurde -
           z.B. beim Austausch des Work-Objektes -, gab es Probleme.
         !end_itemize

 26.03.2002 (!link [ACS.LIB] [Pure-C-Bibliothek]), (!link [ACSPLUS.LIB] [C-Erweiterungs-Bibliothek]),

         !begin_itemize
         !item Die von der Funktion Af_fileselect intern benutzten
           Wrapper-Funktionen fÅr die Funktionen Af_fileselect,
           Af_select, Af_first_fsel und Af_next_fsel wurden komplett
           Åberarbeitet. (!I)Dadurch wurde der Parameter patterns
           geÑndert!(!i)

           In dem Parameter patterns werden jetzt Endungs-Muster
           Åbergeben. Mehrere Endungen in einem Muster werden durch
           ',' getrennt, mehrere Muster durch ein Null-Byte.
           Abgeschlossen werden die Muster durch zwei
           aufeinanderfolgende Null-Bytes. Der (!I)GUI-Editor(!i) verwendet
           z.B. beim ôffnen die Patterns (als C-String)
           "ACS,acs\0RSC,rsc\0\0".

           Falls die FSLX-Funktionen verfÅgbar sind, werden die
           Extensionen direkt verwendet (incl. Groû-/
           Kleinschreibungsvarianten), falls auf Af_select bzw.
           Af_first_fsel und Af_next_fsel zurÅckgegriffen wird, wird
           nur die erste Extension (unverÑndert) verwendet.

         !item Die neue Funktion Ash_getOSHeader liefert einen Zeiger auf
           die Struktur SYSHDR (manchmal auch OSHEADER genannt)
           zurÅck.
         !end_itemize

 20.03.2002 (!link [ACS.LIB] [Pure-C-Bibliothek]), ACSTOOLS.LIB, ACSMOD.LIB, ACSMOD01.LIB

         !begin_itemize
         !item Die neue Funktion Ast_istr sucht analog zur C-Funktion
           strstr einen Substring in einem String - allerdings im
           Gegensatz zu strstr ohne Unterscheidung zwischen Groû- und
           Kleinschreibung. Die Funktion ist auch in der Modul-
           Schnittstelle enthalten.
         !end_itemize

 16.03.2002 (!link [ACS.LIB] [Pure-C-Bibliothek]), ACSTOOLS.LIB

         !begin_itemize
         !item Wenn im Dateinamen das Quote-Zeichen selbst vorkam und
           daher verdoppelt wurde, so funktionierte nichts mehr. Die
           Funktionen Af_quote und Af_unquote arbeiten nun korrekt.
           Als Quote-Zeichen erkennt Af_unquote sowohl die einfachen
           als auch die doppelten AnfÅhrungszeichen, wÑhrend Af_quote
           selbst nur die einfachen AnfÅhrungszeichen verwendet.

         !item Die neue Funktion Af_parseCmdLine zerlegt eine
           Kommandozeile und liefert eine lineare Liste ULinList mit
           den Dateinamen zurÅck. Hierbei wird das Quoting korrekt
           behandelt, d.h. aus den String

                "'Param Leer.txt' Param_no_Leer.txt 'Param Leer
                und `` darin.txt`"

           wird eine lineare Liste mit den EintrÑgen

                Param Leer.txt
                Param_no_Leer.txt
                Param Leer und ` darin.txt

           gebildet. Die Namen liegen also nicht gequoted vor!

         !item Die Dateinamen, die von auûerhalb der Anwendung Åber die
           Drag-Routinen als AT_FILE geliefert werden, liegen nun
           gequoted vor. Ggf. kînnen mehrere Dateinamen geliefert
           worden sein, die Åber getrennte Aufrufe der (!link [Drag-Routine] [Click-Routine])
           Åbergeben werden. Intern wird der gelieferte String Åber
           Af_parseCmdLine in die Dateinamen zerlegt, die dann per
           Af_quote gequoted und an an die (!link [Drag-Routine] [Click-Routine]) bzw. dem
           Fenster mittels der Nachricht AS_DRAGGED weiter gegeben
           werden.

           Damit sollten nun lange Dateinamen - besonders mit
           Leerzeichen oder einfachen AnfÅhrungszeichen - wesentlich
           komfortabler mîglich sein, als es bisher der Fall war.

           Zur Erinnerung: Wenn keines der im C-String " \t\n\r'"
           enthaltenen Zeichen im Åbergebenen Dateinamen vorkommt, so
           macht Af_quote nichts!
         !end_itemize

 14.03.2002 (!link [ACS.LIB] [Pure-C-Bibliothek]), (!link [ACSPLUS.LIB] [C-Erweiterungs-Bibliothek])

         !begin_itemize
         !item Bei meinen Tests habe ich leider feststellen mÅssen, daû
           das ôffnen von Dateien im PUR_DESK nun nicht mehr
           reibungslos klappte. Jetzt geht (endlich) sowohl das ôffnen
           im PUR_DESK, als auch die öbergabe von Dateien per AV-
           (!link [Protokoll] [Protokoll]) oder Drag&Drop-Protokoll
         !end_itemize

 03.03.2002 (!link [ACS.LIB] [Pure-C-Bibliothek]), (!link [ACSPLUS.LIB] [C-Erweiterungs-Bibliothek])

         !begin_itemize
         !item Der PUR_DESK und der DESKTOP unterstÅtzen nun Quoting von
           Dateinamen auch bei Åbergebenen Namen beim AV-(!link [Protokoll] [Protokoll])
           oder Atari-Drag&Drop-(!link [Protokoll] [Protokoll]).

         !item Beim Drag&Drop-(!link [Protokoll] [Protokoll]) war noch ein Fehler enthalten, so
           daû eine Parameter-öbergabe von z.B. jinnee an ACS-
           Anwendungen nicht richtig funktionierte.

         !item Bei meinen Tests der Parameter-öbergabe stellte ich
           erstaunt fest, daû jinnee beim ôffnen von mehreren ACS-
           Dateien (im jinnee-Fenster z.B. zwei Dateien selektiert und
           "Datei\Objekte îffnen" auswÑhlen) den ersten Parameter an
           den (!I)GUI-Editor(!i) Åbergeben hat und den zweiten je nach
           Anmeldung des (!I)GUI-Editor(!i) per VA-START oder Drag&Drop
           Åbergab. In der frÅhen Initialisierungsphase von (!I)ACSpro(!i)
           ging das schief und wurde mit einem Crash quittiert, der
           nun ausgerÑumt ist.
         !end_itemize

 01.03.2002 (!link [ACSPLUS.LIB] [C-Erweiterungs-Bibliothek])

         !begin_itemize
         !item Sowohl der PUR_DESK als auch der DESKTOP verwenden nun
           Af_unquote vor Aufruf der der hinterlegten Neu-Routine.
         !end_itemize

 28.02.2002 (!link [ACS.LIB] [Pure-C-Bibliothek]), ACSTOOLS.LIB, ACSMOD.LIB, ACSMOD01.LIB

         !begin_itemize
         !item Die neue Funktion Af_quote prÅft, ob ein Dateiname mit
           Quote-Zeichen versehen muû, bevor er an andere Anwendungen
           gesandt werden kann (z.B. fÅr AV-(!link [Protokoll] [Protokoll])). Umgekehrt
           entfernt ggf. die Funktion Af_unquote vorhandene Quote-
           Zeichen.
         !end_itemize

 24.02.2002 ACSMOD.LIB, ACSMOD01.LIB

         !begin_itemize
         !item Die neuen Funktionen fÅr die KEYTAB-Ansteuerung sowie das
           Setzen der Icons sind in die Modul-Schnittstelle
           aufgenommen worden.
         !end_itemize

 24.02.2002 (!link [ACS.LIB] [Pure-C-Bibliothek]), ACSTOOLS.LIB

         !begin_itemize
         !item Der Druck-Dialog sowie der File- und der Font-Selector
           verwenden nun kein Icon mehr. öber die Funktionen
           Ash_printSetIcon, Ash_fileSetIcon und Ash_fontSetIcon kann
           das zu verwendende Icon gesetzt oder durch NULL bzw. NIL
           gelîscht werden. Der Parameter ghost setzt oder lîscht bei
           vorhandenem Icon das "Ghost-Flag" (!link [AW_GHOSTICON] [AW_GHOSTICON]).

         !item Die Bibliothek KEYTAB.LIB wurde in (!I)ACSpro(!i) integriert.
           Mittels dieser Bibliothek wird KEYTAB zur Zeichensatz-Kon-
           version angesteuert. Die dazu notwendigen Funktionen sind

                Akt_getKeyTab, Akt_getRelease, Akt_getEuro,
                Akt_getExpMaxNr, Akt_getImpMaxNr,
                Akt_getExpNameFromNr, Akt_getImpNameFromNr,
                Akt_getExpShortNameFromNr,
                Akt_getImpShortNameFromNr, Akt_getExpNrFromId,
                Akt_getImpNrFromId, Akt_getExpIdFromNr,
                Akt_getImpIdFromNr, Akt_getExpNrFromName,
                Akt_getImpNrFromName, Akt_CharAtari2X,
                Akt_CharX2Atari, Akt_BlockAtari2X,
                Akt_BlockX2Atari, Akt_StringAtari2X und
                Akt_StringX2Atari.

           In der Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek]) sind zusÑtzlich die Funktionen

                Akt_popExpName, Akt_popImpName, Akt_chooseExpNr
                und Akt_chooseImpNr

           enthalten, die eine komfortable Auswahl des Ziel- bzw.
           Quell-Zeichensatzes Åber Popups ermîglichen.

           Die Einbindung der Bibliothek KEYTAB.LIB oder der Header-
           Datei KEYTAB.H ist damit ÅberflÅssig geworden.

           Genauere Informationen Åber KEYTAB und die KEYTAB.LIB
           liegen zusammen mit allen(!) Sourcen KEYTAB bei. KEYTAB
           kann aus der Maus LL (Telefonnummer 0 81 92 / 99 6 99 99,
           V.34 und X.75) oder vom Internet unter
           http://acspro.atari.org/ geladen werden.
         !end_itemize

 15.12.2001 ACSTOOLS.LIB

         !begin_itemize
         !item Es gibt nun eine weitere Bibliothek: ACSTOOLS.LIB. Sie ist
           fÅr TOS/TTP-Programme und stellt einige Utility-Funktionen
           aus (!I)ACSpro(!i) zur VerfÅgung.

           Auch hier erfolgt die Initialisierung Åber die bekannten
           Funktionen ACSinit0 und ACSinit. Die wichtigsten
           Initialisierungs-Daten mÅssen Åber eine Variable ACSconfig
           vom Typ Aconfig zur VerfÅgung gestellt werden. Die
           Dokumentation dieser Bibliothek wird in die (!I)ACSpro(!i)-
           Dokumentation eingearbeitet werden.

           (!B)Hinweis:(!b) Die Struktur von Aconfig, Ablk etc. ist fÅr die
           TOS-/TTP-Programme entsprechend angepaût worden, d.h. GEM-
           Anwendungen benîtigen (!link [ACS.LIB] [Pure-C-Bibliothek]), TOS/TTP-Programme
           ACSTOOLS.LIB. Beide Bibliotheken schlieûen sich aus.
         !end_itemize

 28.11.2001 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Beim Erstellen der Cfg-Struktur UConfig wurde die
           Komponente env_praefix der Info-Struktur UCfgInfo nicht
           korrekt ausgewertet: wenn env_praefix gleich NULL bzw. NIL
           ist, so wurde ein leerer String erzeugt. Durch env_praefix
           gleich NULL bzw. NIL wird signalisiert, daû (!I)nicht(!i) im
           Environment nach einer Cfg-Variablen gesucht werden soll,
           wenn sie in den Cfg-Strings nicht gefunden wird.
         !end_itemize

 27.11.2001 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Nach einigen Tests hat sich ein Åbler Absturz in den Cfg-
           Strings gezeigt, der nun korrigiert ist. Er trat auf, wenn
           ein gesuchter String nicht gefunden wurde und versucht
           wurde, im Environment den gewÅnschten Wert zu finden.
         !end_itemize

 26.10.2001 (!link [ACSPLUS.LIB] [C-Erweiterungs-Bibliothek])

         !begin_itemize
         !item Der PUR_DESK wurde um ein paar interne öberprÅfungen
           ergÑnzt, die den Betrieb gegenÅber Programmier-Fehlern
           stabiler machen dÅrften.
         !end_itemize

 17.10.2001 (!link [ACS.LIB] [Pure-C-Bibliothek]), ACSMOD.LIB, ACSMOD01.LIB

         !begin_itemize
         !item Die neue Funktion Ash_gettimer liefert den Wert des 200Hz-
           Timers zurÅck. Bevorzugt wird der Wert Åber Ssystem
           ermittelt, falls dies nicht geht, wird der Timer direkt
           ausgelesen. Die Funktion steht auch Åber die Modul-
           Schnittstelle zur VerfÅgung.
         !end_itemize

 25.09.2001 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item In (!I)ACSpro(!i) wurden die énderungen aus PCGEM.LIB Åbernommen
           (Binding v_opnwk erweitert, Binding fÅr v_copies
           nachgerÅstet).
         !end_itemize

 18.09.2001 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Kleinere Umstellungen und Optimierungen bei der
           Initialisierung des ACS-Systems - z.B. sollte jetzt nach
           der Initialisierung der normale Mauszeiger erscheinen.

         !item Kleinere Optimierungen in Amo_busy() und Amo_unbusy() sowie
           Amo_hide() und Amo_show().
         !end_itemize

 06.09.2001 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Der Font-Selektor hatte keinen Fenster-Titel. Dieser wird
           jetzt an die Funktion Ash_font Åbergeben. Daher erhÑlt die
           Funktion Ash_font als neuen ersten Parameter den Fenster-
           Titel, die restlichen Parameter sind wie gewohnt.

         !item Beim Aufruf der Druck-Dialog im nicht-modalen Fenster
           erfuhr man in der Nachricht AS_PRINT, welche Daten
           eingestellt wurden. Allerdings fehlte hier die Information,
           ob "OK", "Drucken" oder "Abbrechen" gewÑhlt worden war.
           Daher wurde die Struktur A_PrintSel um die Angabe des
           Buttons erweitert. Wenn der Button nicht PDLG_OK ist, so
           wurde Abbrechen gewÑhlt, bei PDLG-OK entweder OK oder
           Drucken, was durch options (der Wert, der Ash_print
           mitgegeben wurde) bestimmt wird.
         !end_itemize

 21.08.2001 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Wenn in Fenster-Dialogen ein Tastendruck nicht verarbeitet
           werden kann - z.B. weil kein Eingabefeld existiert und es
           kein passendes TastenkÅrzel gibt -, so passierte gar
           nichts.

           Jetzt kann bei TastendrÅcken ohne Ctrl oder Alt versucht
           werden, ein TastenkÅrzel mit Ctrl oder Alt zu verwenden.
           öber die Variable ACSblk->keyAltCtrlSwitch wird global fÅr
           die Anwendung das Verhalten definiert. Folgende Varianten
           sind mîglich:

              !begin_itemize
              !item Suche im Work-Objekt getrennt einschaltbar

              !item Suche in der Toolbar getrennt einschaltbar

              !item Suche nach Alt+Key getrennt einschaltbar

              !item Suche nach Ctrl+Key getrennt einschaltbar

              !item Suche nach Ctrl+Alt+Key getrennt einschaltbar

              !item Zuerst im Work-Objekt suchen, dann in der Toolbar

              !item Zuerst in der Toolbar suchen, dann im Work-Objekt
              !end_itemize

           HierfÅr sind einige Konstanten definiert:

            !begin_xlist [KEY_AC_CTRL_FIRST_WORK]
            !item [KEY_AC_NONE]            bisheriges Verhalten (keine
                                   Variante probieren)

            !item [KEY_AC_ALT_WORK]        Alt+Key im Work-Objekt versuchen
                                   (defaultmÑûig aktiviert)

            !item [KEY_AC_CTRL_WORK]       Ctrl+Key im Work-Objekt versuchen
                                   (defaultmÑûig nicht aktiviert)

            !item [KEY_AC_CTRL_FIRST_WORK] FÅr das Work-Objekt erst Ctrl+Key,
                                   dann Alt+Key versuchen (Standard:
                                   erst Alt, dann Ctrl)

            !item [KEY_AC_ALT_CTRL_WORK]   Ctrl+Alt+Key im Work-Objekt
                                   versuchen (defaultmÑûig nicht
                                   aktiviert)

            !item [KEY_AC_ALT_TOOL]        Alt+Key in der Toolbar versuchen
                                   (defaultmÑûig nicht aktiviert)

            !item [KEY_AC_CTRL_TOOL]       Ctrl+Key in der Toolbar versuchen
                                   (defaultmÑûig nicht aktiviert)

            !item [KEY_AC_CTRL_FIRST_TOOL] FÅr die Toolbar erst Ctrl+Key, dann
                                   Alt+Key versuchen (Standard: erst
                                   Alt, dann Ctrl)

            !item [KEY_AC_ALT_CTRL_TOOL]   Ctrl+Alt+Key in der Toolbar
                                   versuchen (defaultmÑûig nicht
                                   aktiviert)

            !item [KEY_AC_TOOL_FIRST]      Zuerst in der Toolbar, dann im
                                   Work-Objekt nachsehen (Standard:
                                   erst Work-Objekt, dann Toolbar)
            !end_xlist
         !end_itemize

 08.08.2001 (!I)GUI-Editor(!i)

         !begin_itemize
         !item Das Generieren der Sourcen wurde korrigiert. Wenn in
           Strings Sonderzeichen wie z.B. Linefeed oder Formfeed
           enthalten waren, so sind diese direkt so ausgegeben worden.

              !begin_itemize
              !item In C-Strings werden diese Zeichen korrekt als Escape-
                Sequenz ausgegeben. Beispielsweise wird der String
                "Zeilen-umbruch" mit dem Zeichen '\n' nach dem
                Trennstrich (lÑût sich hier nicht passend wiedergeben)
                dann korrekt als "Zeilen-\numbruch" geschrieben.

              !item FÅr Pascal werden diese Zeichen bis auf das Pascal-
                Zeichen "'" Åber ihren ASCII-Code in den String
                geschrieben.
              !end_itemize
         !end_itemize

 25.06.2001 (!link [ACSPLUS.LIB] [C-Erweiterungs-Bibliothek])

         !begin_itemize
         !item Die Funktionen wputs, wprintf und wvprintf prÅfen nun, ob
           das Åbergebene Fenster ungleich NULL bzw. NIL ist.

           (!B)Tip:(!b) Damit lassen sich Debug-Ausgaben in ein Protokoll-
           Fenster umleiten. FÅr die Release-Version wird das globale
           Debug-Fenster nicht mehr angelegt, sondern die globale
           Debug-Fenster-Variable auf NULL bzw. NIL gesetzt - und
           schon sind alle Debug-Ausgaben quasi wirkungslos.
         !end_itemize

 14.06.2001 (!I)GUI-Editor(!i)

         !begin_itemize
         !item Beim Zeichnen von Icons im Icon-Editor nimmt der Maus-Pfeil
           die gewÑhlte Farbe an. Bei hellen Farben - speziell
           Hellgrau - war der Maus-Zeiger auf dem hellen Untergrund
           nicht mehr erkennbar. Jetzt wird anhand einer empirischen
           "Helligkeits-Bestimmung" (Der Name klingt nach wesentlich
           mehr, als es ist) die Umrahmung des Maus-Zeigers bei
           dunklen Farben auf weiû, bei hellen Farben auf schwarz
           gestellt.
         !end_itemize

 05.06.2001 (!link [ACSPLUS.LIB] [C-Erweiterungs-Bibliothek])

         !begin_itemize
         !item Der (!link [Slider] [Slider])-Balken wird nun auch wie die Pfeile deaktiv
           dargestellt, wenn der (!link [Slider] [Slider]) den gesamten Bereich bereits
           anzeigt, d.h. wenn der Balken bisher nicht zu verschieben
           war.
         !end_itemize

 01.06.2001 ACSMOD.LIB, ACSMOD01.LIB

         !begin_itemize
         !item Die Funktionen Acfg_getChar, Acfg_setChar, Acfg_getLong,
           Acfg_setLong, Acfg_getBool, Acfg_setBool und Acfg_getString
           sind auch in der Modul-Schnittstelle enthalten.
         !end_itemize

 01.06.2001 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Die (!link [Cfg-Strings] [Die Konfigurations-Strings]) haben die neuen Funktionen Acfg_setChar,
           Acfg_getChar, Acfg_getLong, Acfg_setLong, Acfg_getBool und
           Acfg_getBool erhalten. Diese nehmen ggf. die Umwandlung der
           Werte von char, INT32 bzw. Logischen Werten in Strings und
           umgekehrt vor.

         !item Die neue Funktion Acfg_getString stellt eine AbkÅrzung zu
           Acfg_getValue dar, der Wert wird in einem statischen String
           zurÅckgegeben.

           (!B)Hinweis:(!b) Die Funktion Acfg_getString ist (!I)nicht(!i) multi-
           threading-fest!

         !item Die neue Funktion Acfg_clearGroup lîscht eine einzige
           Gruppe.

         !item Die Funktion Acfg_clearGroup wurde in Acfg_clearAllGroups
           umbenannt.
         !end_itemize

 27.05.2001 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Es gibt zwei neue Funktionen, um zu ÅberprÅfen, ob es eine
           Parameter-Gruppe bzw. einen Cfg-String gibt.
           Acfg_isGroupPresent liefert TRUE, wenn die genannte Gruppe
           existiert, anderfalls wird FALSE geliefert. Ebenso liefert
           Acfg_isStringPresent bei existierendem Parameter TRUE,
           andernfalls FALSE.

           (!B)Hinweis:(!b) Die beiden Funktionen betrachten nur die
           Åbergebene UConfig-Struktur, nicht evtl. von Acfg_getValue
           untersuchte Environment-Strings!

         !item Die Funktionen Acfg_getBool und Acfg_setBool verwenden nun
           andere Werte. Acfg_setBool gibt dem Åbergebenen Parameter
           je nach Flag die Werte "True" oder "False". Acfg_getBool
           konvertiert den Wert "True" nach TRUE, alles andere wird
           FALSE.
         !end_itemize

 25.05.2001 ACSMOD.LIB, ACSMOD01.LIB, ACS_LOAD.O

         !begin_itemize
         !item Die Modul-Schnittstelle ist nun um die kompletten Bindings
           der VDI- und AES-Funktionen erweitert worden. Damit wird
           das applikations-globale AES-Array ACSblk->AESglobal bei
           AES-Aufrufen der Module benutzt, was bislang bei Modulen zu
           Problemen gefÅhrt hat (Module hatten bislang die GEM-
           Bibliothek ebenfals gelinkt und damit ein eigenes und damit
           uninitialisiertes global-Array benutzt).
         !end_itemize

 06.05.2001 (!I)GUI-Editor(!i)

         !begin_itemize
         !item Der (!I)GUI-Editor(!i) hat bislang in die exportierte UConfig-
           Struktur, die in der exportierten Variable ACSconfig des
           Hauptmoduls enthalten ist, als Dateiname der Cfg-Strings
           "PUR_DESK" geschrieben. Nun wird dieser Text leer gelassen.

           Damit wird fÅr das Laden der in ACSblk->basename stehende
           Name verwendet. In spÑteren Versionn ist geplant, die in
           ACSconfig enthaltene Struktur CfgInfo auch im (!I)GUI-Editor(!i)
           bereits bearbeiten zu kînnen.
         !end_itemize

 06.05.2001 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Die (!link [Cfg-Strings] [Die Konfigurations-Strings]) werden nun beim Anwendungs-Start
           automatisch geladen. Die Cfg-Strings stehen danach in
           ACSblk->cfg zur VerfÅgung und kînnen mit den zugehîrigen
           Funktionen bearbeitet und ausgewertet werden.

           Der Dateiname fÅr das Laden wird aus ACSconfig genommen.
           Falls der dort notierte Name leer sein sollte, wird der
           Dateiname anhand ACSblk->basename gebildet:
           Af_cfgfile(&ACSblk->basename[2]) (die 2 wegen der zwei
           fÅhrenden Leerzeichen in ACSblk->basename).

           Das Laden der Cfg-Strings geschieht unmittelbar nach Aufruf
           der ACSinit0-Funktion, so daû der dort ggf. manipulierte
           Name in ACSblk->basename verwendet wird.
         !end_itemize

 28.04.2001 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Das Thermometer kann nun auch nicht-modal aufgerufen
           werden, indem der Funktion Ash_thermometer im Parameter
           kind das neue Flag THERMO_NONMODAL Åbergeben wird.

           (!B)Hinweis:(!b)Das modale Thermometer setzt allerdings den Wert
           fÅr ACSblk->ev_mtcount auf 1, damit die Schleife
           schnellstmîglich durchlÑuft. Dies passiert beim nicht-
           modalen Thermometer natÅrlich nicht. Damit lÑuft das
           Thermometer nicht mehr gleichmÑûig, wenn z.B. die Maus
           bewegt wird. Normalerweise wird wohl auûerdem das modale
           Thermometer zum Einsatz kommen... ;-)
         !end_itemize

 27.04.2001 (!link [ACSPLUS.LIB] [C-Erweiterungs-Bibliothek])

         !begin_itemize
         !item Der (!link [Slider] [Slider])-Balken wird nun wie die Pfeile deaktiv
           dargestellt, wenn das Objekt im Status DISABLED ist.
         !end_itemize

 22.04.2001 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Die Funktion Aev_WmRedraw verschickt eine Redraw-Nachricht.
           (!I)ACSpro(!i) verwendet diese Funktion, um in einigen FÑllen ein
           Neuzeichnen von Fenstern oder sogar des gesamten
           Bildschirmes zu veranlassen (z.B. in Awi_layout).

         !item Die Funktion Awi_alert hat den Alert-String direkt an
           form_alert weitergegeben, wenn der (!I)ACSpro(!i)-eigene modale
           Alert-Fenster-Dialog nicht verwendet werden konnte. Nun
           wird das Icon '4' auf das Icon '1' fÅr form_alert
           umgesetzt.
         !end_itemize

 19.04.2001 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Die Funktionen Awi_redraw zeichnet den Fenster-Inhalt nur
           noch, wenn das Fenster nicht im Zustand (!link [AWS_SHADED] [AWS_SHADED]) ist. Da
           hin und wieder die Redraw-Message vor dem "Unshaden"
           eintrifft, wird bei Erhalt der Nachricht WM_UNSHADED das
           Fenster davon informiert, das Flag (!link [AWS_SHADED] [AWS_SHADED]) gelîscht und
           dann ein Neuzeichnen angestoûen.

         !item Die Funktionen Awi_obchange und Awi_obredraw rufen bei
           gesetztem Flag (!link [AWS_SHADED] [AWS_SHADED]) direkt die normale Redraw-Routine
           des Fensters auf. Diese ist in den meisten FÑllen
           Awi_redraw, welche in diesem Zustand die Arbeit verweigert
           - das Fenster ist ja eingeklappt und nichts auûer der
           Titelzeile zu sehen.
         !end_itemize

 17.04.2001 (!I)GUI-Editor(!i)

         !begin_itemize
         !item Der (!I)GUI-Editor(!i) îffnet nun Module nicht mehr als Text im
           Editor, sondern startet sie per Ash_module nach.

         !item Im Eigenschaften-Dialog jeder ACS-Datei wurden bereits die
           Optionen fÅr das Exportieren und Importieren der Texte
           vorgesehen. Das Importieren der Texte sowie das
           Zusammenbinden und Verwenden der generierten Texte wird
           noch nicht in Version 3.0.0 enthalten sein.
         !end_itemize

 17.04.2001 (!link [ACSPLUS.LIB] [C-Erweiterungs-Bibliothek])

         !begin_itemize
         !item Der pure Desktop verwendet nun zum Nachladen von Modulen
           die Funktion Af_fileselect, d.h. die Auswahlbox wird,
           sofern mîglich, in ein modales Fenster gelegt, Multi-
           Selektion wird ebenfalls unterstÅtzt.
         !end_itemize

 14.04.2001 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Die neue Funktion Ast_reverse dreht einen String
           zeichenweise um, d.h. im Åbergebenen Puffer ist das letzte
           Zeichen das erste und das erste das letzte.

         !item Die Funktion Aob_visible prÅft, ob ein Objekt im Objektbaum
           sichtbar ist, oder ob eines der Eltern-Objekte unsichtbar
           ist.

         !item Ebenso prÅft die Funktion Awi_obvisible, ob das angegebene
           Objekt im Fenster sichtbar sein kînnte, d.h. ob das Fester
           geîffnet, nicht ikonifiziert und nicht "shaded" ist und
           keines der Eltern-Objekte des Objektes unsichtbar ist. Es
           wird nicht geprÅft, ob der passende Ausschnitt im Fenster
           sichtbar ist.
         !end_itemize

 14.04.2001 (!I)GUI-Editor(!i)

         !begin_itemize
         !item Der (!I)GUI-Editor(!i) generiert beim Erzeugen der Dateien fÅr den
           Compiler nun auch eine neue Datei fÅr die Strings. Diese
           Datei ist im Format der (!link [Cfg-Strings] [Die Konfigurations-Strings]) aufgebaut. Pro ACS-
           Datei wird eine ASCII-Datei generiert, wobei der Dateiname
           aus dem Namen der ACS-Datei durch Ersetzen von ".acs" durch
           ".st<n>" hervorgeht - <n> ist hierbei eine Zahl von 0 bis 3
           je nach aktueller Sprach-Einstellung. Dies vereinfacht das
           öbersetzen von Anwendungen erheblich, da beim Editieren der
           Texte nur die aktuelle Sprache verwendet wird und nun die
           generierte ASCII-Datei der Original-Sprache parallel in den
           Editor geladen werden kann.

           Diese ASCII-Dateien werden bislang nur erzeugt und noch
           nicht verwendet. Es ist geplant, diese ASCII-Dateien zu
           einer einzigen Datei pro Anwendung zusammenzusetzen
           (entweder durch ein extra Merge-Programm oder durch den
           (!I)GUI-Editor(!i). Diese eine groûe Datei soll dann die Endung
           ".str" haben, im Verzeichnis der compilierten Anwendung
           stehen, alle Strings der Anwendung enthalten und Åber die
           Mechanismen der (!link [Cfg-Strings] [Die Konfigurations-Strings]) ACS-intern verwendet werden.
           Ein Umschalten der Sprache soll dann durch Austausch dieser
           Datei gegen eine Datei mit Åbersetzten Texten mîglich sein.
           Auch das bisherige Verfahren, die Strings in die Anwendung
           zu compilieren (quasi als Default-Strings), soll mîglich
           sein. Aber das ist noch Zukunftsmusik (siehe ToDo-Liste).
         !end_itemize

 07.04.2001 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Die Nachricht WM_MOUSEWHEEL wird nun ausgewertet.
           Allerdings wird "nur" das Fenster gescrollt. Da es nur eine
           Scroll-Information gibt, wird zuerst versucht, vertikal zu
           scrollen. Wenn es die Scroll-Elemente bei dem Fenster nicht
           gibt oder bereits alles dargestellt werden sollte, wird
           versucht, horizontal zu scrollen, sofern dies mîglich ist.
           Wenn die Control-Taste gedrÅckt ist, wird das Verhalten
           umgedreht, d.h. erst wird versucht, horizontal zu scrollen,
           dann ggf. vertikal.
         !end_itemize

 03.04.2001 ACSMOD.LIB

         !begin_itemize
         !item Die Modul-Schnittstelle hat eine abwÑrtskompatible
           Schnittstellenfunktion bekommen, die die Schrittweite 1 fÅr
           die Funktion Awi_arrowed bei Modulen von ACS 2.3x einsetzt.
           Module, die bereits mittels Version 3.0.0 geschrieben
           wurden, mÅssen allerdings angepaût werden.
         !end_itemize

 03.04.2001 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item (!I)ACSpro(!i) unterstÅtzt nun das erweiterte Scrollen gemÑû WINX.
           Dazu ist einiges passiert...

         !item Die Routine Awi_arrowed hat als dritten Parameter die
           Scroll-Schrittweite bekommen und berÅcksichtigt diesen
           auch.

         !item Die Fenster-Routine (!link [Awindow.arrowed] [Awindow.arrowed]) hat ebenfalls diesen
           Parameter bekommen.
         !end_itemize

 29.03.2001 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item (!I)ACSpro(!i) beherrscht nun auch die von NKCC gewohnten sog.
           (!link [Dead-Keys] [Dead-Keys]). Wie funktionieren diese? Ganz einfach: nach
           Eingabe eines (!link [Dead-Keys] [Dead-Keys]) geschieht gar nichts, der
           Tastendruck wird zunÑchst gepuffert und scheinbar
           ignoriert. Beim nÑchsten Zeichen wird dann versucht, das
           erste und das zweite Zeichen zu einem einzigen Zeichen zu
           kombinieren. Genauer lÑuft hier folgendes ab:

             !begin_enumerate
             !item Wenn kein Dead-Key gepuffert ist, wird geprÅft, ob ein
                Dead-Key eingegeben wurde. Ein Dead-Key stammt aus der
                Menge ^ '`π"¯,/ und muû freigeschaltet sein (s.u.).

             !item Falls kein Dead-Key eingegeben wurde oder dieser
                gesperrt sein sollte, so wird das Zeichen normal
                weitergereicht.

             !item Handelt es sich jedoch bei dem Zeichen um einen
                freigegebenen Dead-Key, so wird das Zeichen gepuffert
                und die Verarbeitung des Tastendruckes abgebrochen.
                (!I)ACSpro(!i) wartet nun auf den nÑchsten Tastendruck.

             !item Wenn ein Dead-Key gepuffert ist, so wird
                nachgeschlagen, ob fÅr die vorliegende Kombination
                Dead-Key plus das neu eingegebene Zeichen eine
                Ersetzung hinterlegt ist. Wenn dies der Fall ist, wird
                das zuletzt eingegebene Zeichen durch das Ersatz-
                Zeichen ersetzt. Andernfalls wird zuerst der
                gepufferte Dead-Key und danach das zweite Zeichen in
                die normale Tastatur-Verarbeitung gespielt - wie wenn
                es keine (!link [Dead-Keys] [Dead-Keys]) gÑbe. Der Dead-Key-Puffer ist nach
                dieser Verarbeitung wieder leer.
             !end_enumerate

           Die (!link [Dead-Keys] [Dead-Keys]) mÅssen natÅrlich individuell freigeschaltet
           werden. Hierzu wird in der neuen Variable ACSblk->ev_mkdead
           die Zeichen eingetragen, die die Anwendung als (!link [Dead-Keys] [Dead-Keys])
           betrachtet haben mîchte. Wie in der ToDo-Liste vermerkt,
           wird dies als Erweiterung des (!I)GUI-Editor(!i) vorgesehen.

           Folgende Ersetzungs-Varianten werden unterstÅtzt:


!begin_table [lclll]
        Akzent       !! Dead-Key  !!    !! Zweite Zeichen  !! Ersetzung
!hline
        Circumflex   !!    ^      !! +  !! aeiou           !! =  Éàåìñ
        Tilde        !!    !~     !! +  !! nNaoAo          !! =  §•∞±∑∏
        Akut         !!    '      !! +  !! eEaiou          !! =  Çê†°¢£
        Gravis       !!    `      !! +  !! aeiouA          !! =  Öäçïó∂
        Umlaut       !!    π      !! +  !! aeiouyAOU       !! =  ÑâãîÅòéôö
        Umlaut       !!    "      !! +  !! aeiouyAOU       !! =  ÑâãîÅòéôö
        Angstrîm     !!    ¯      !! +  !! aA              !! =  Üè
        Cedille      !!    ,      !! +  !! cC              !! =  áÄ
        kein Akzent  !!    /      !! +  !! oO24            !! =  ≥≤´¨
!end_table

           Die fÅr eine Anwendung zu verwendenden (!link [Dead-Keys] [Dead-Keys]) mÅssen per
           Programm z.B. in ACSinit0 oder ACSinit in ACSblk->ev_mkdead
           eingetragen werden. In spÑteren ACS-Versionen wird dies
           bereits im (!I)GUI-Editor(!i) mîglich sein.
         !end_itemize

 17.03.2001 (!link [ACSPLUS.LIB] [C-Erweiterungs-Bibliothek])

         !begin_itemize
         !item Auch die Toolbars der anderen Fensters wurden etwas zu
           klein, wenn der Systemzeichensatz zu klein eingestellt war.
           Jetzt ist die Grîûe aller Toolbars von der Grîûe des
           Systemzeichensatzes unabhÑngig.
         !end_itemize

 16.02.2001 (!link [ACSPLUS.LIB] [C-Erweiterungs-Bibliothek])

         !begin_itemize
         !item Die neue Funktion Awi_layout schaltet ggf. die Flags
           AB_NO3D und AB_OLDMENU um. Beim Umschalten des Flags
           AB_OLDMENU werden - nach RÅckfrage beim jeweiligen Fenster
           Åber die Nachricht AS_CHANGE_LAYOUT - alle MenÅs auf das
           jeweils gewÅnschte Layout umgestellt.
         !end_itemize

 16.02.2001 (!link [ACSPLUS.LIB] [C-Erweiterungs-Bibliothek])

         !begin_itemize
         !item Das Editor-Fenster EDITOR hat beim Kopieren auf das
           Klemmbrett den gesamten und nicht nur den markierten Text
           geschrieben. Das sollte nun abgestellt sein...
         !end_itemize

 12.02.2001 ACSMOD.LIB, ACSMOD01.LIB

         !begin_itemize
         !item Die Funktionen Aev_DhstAdd und Aev_DhstSaved stehen nun
           auch in der Modul-Schnittstelle zur VerfÅgung.
         !end_itemize

 10.02.2001 (!I)GUI-Editor(!i)

         !begin_itemize
         !item Auch der (!I)GUI-Editor(!i) verschickt mittels Aev_DhstAdd die
           Namen der gespeicherten ACS-Dateien an den DHST-Server.
         !end_itemize

 10.02.2001 (!link [ACSPLUS.LIB] [C-Erweiterungs-Bibliothek])

         !begin_itemize
         !item Der Editor informiert nun beim Speichern nicht nur den
           OLGA-Manager, sondern auch den DHST-Server Åber die
           gespeicherte Datei.
         !end_itemize

 10.02.2001 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Die Funktion Aev_DhstSaved ermittelt aus dem Åbergebenen
           Dateinamen incl. Pfad den reinen namen incl. Endung - z.B.
           "Doku.TXT" - und verwendet diesen bei einem Aufruf von
           Aev_DhstAdd als Dokumentennamen.

         !item (!I)ACSpro(!i) beherrscht nun das DHST-Protokoll, bei dem ein
           Document-History-Server (z.B. SMU) Åber die letzten
           gesicherten Dateien informiert wird.

           Die neue Funktion Aev_DhstAdd sendet Dokument-Name und
           -Pfad an den DHST-Server. Der Anwendungsname wird aus
           ACSblk->AppLongName, der Anwendungspfad aus ACSblk->appname
           verwendet. Die komplette Kommunikation mit dem DHST-Server
           Åbernimmt (!I)ACSpro(!i).
         !end_itemize

 05.02.2001 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Der Bug beim nicht-modalen WDIALOG-Druckdialog ist nun
           behoben. Der Druckdialog steht also "in voller Schînheit"
           zur VerfÅgung.
         !end_itemize

 03.02.2001 ACSMOD.LIB, ACSMOD01.LIB

         !begin_itemize
         !item Die Funktionen Ash_font und Ash_print stehen nun auch Åber
           die Modul-Schnittstelle allen Modulen zur VerfÅgung.

         !item Die Funktion (!link [A_fileselect] [Ash_fileselect]) wurde auch in der Modul-
           Schnittstelle in Ash_fileselect umbenannt.
         !end_itemize

 03.02.2001 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Die neue Funktion Ash_font îffnet den (MagiC-)Font-
           Selector. Je nach Parametern kann der Font-Selector als
           modales oder auch nicht-modales Fenster geîffnet werden.
           Wenn der Font-Selector als nicht-modales Fenster lÑuft,
           wird beim Schlieûen des Selektors dem beim Aufruf
           mitgegebenen Fenster die Nachricht AS_FONT zusammen mit den
           gewÑhlten Daten Åber die Struktur A_FontSel mitgegeben.

           Auch hier ist es nicht unbedingt der MagiC-Font-Selector,
           es ist der Font-Selector, der Åber die FNTS-Funktionen
           bedient wird... ;-) Daraus folgt aber auch: ohne FNTS-
           Funktionen (Abfrage erfolgt per appl_xgetinfo) gibt es hier
           nichts!

         !item Die Funktion A_print wurde konsequenterweise in Ash_print
           umbenannt.

         !item Ebenso wurde die Funktion (!link [A_fileselect] [Ash_fileselect]) in Ash_fileselect
           umbenannt.

         !item Der Parameter flag3D in der Funktion Ash_print ist
           entfallen. Die 3D-Einstellung wird nun aus ACSblk-
           >description->flags&AB_NO3D gewonnen - eine konsistentere
           Umsetzung...
         !end_itemize

 01.02.2001 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Neue Klick-Routine Aob_help fÅr beliebige Objekte, die
           einfach die ST-Guide-Hilfe-Seite des Fensters anzeigen
           lÑût. Damit lassen sich "Hilfe-Buttons" blitzschnell
           realisieren.

         !item Bei der Umwandlung der MenÅs in das "moderne Layout" wird
           vor der Umwandlung des Hintergrundes auf den 3D-Effekt per
           appl_xgetinfo(13,...) geprÅft, ob das System die AES-4-
           Hintergrund-Flags kennt. Dies ist nun nicht mehr von der
           Anzahl der Farben abhÑngig. Falls das System die AES-4-
           Hintergrund-Flags nicht kennt, wird das Flag FL3DBAK nicht
           gesetzt, die Umwandlung der MenÅ-Trenner "-" in Linien wird
           denoch durchgefÅhrt.
         !end_itemize

 19.01.2001 (!link [ACSPLUS.LIB] [C-Erweiterungs-Bibliothek])

         !begin_itemize
         !item Schreibfehler in der englischen Version im PUR_DESK
           korrigiert, es heiût nun einmal "module"... ;-)
         !end_itemize

 19.01.2001 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Der PUR_DESK leitet nun komplett alle Nachrichten auûer
           AS_TERM an das Module-Fenster zur Bearbeitung weiter.

         !item Das Module-Fenster versteht nun die vom DESKTOP her
           bekannte Nachricht AS_BACKOBJECT. Damit kann der
           Hintergrund im Module-Fenster wie vom DESKTOP her gewohnt
           verÑndert werden. Das Module-Fenster Åbernimmt die Werte
           ob_type und ob_spec des Åbergebenen Objektes fÅr den
           Hintergrund.

           (!B)Hinweis:(!b) Als Objekt sollte bei der Nachricht AS_BACKOBJECT
           im Moment nur eines verwendet werden, das keine
           Substrukturen (Strings, etc.) hat, da keine Unterstrukturen
           dynamisch neu angelegt werden. Es wÅrde sonst zu Problemen
           beim Terminieren des Module-Fensters (am Programm-Ende)
           fÅhren, da Daten, die nicht alloziert wurden, per Ax_free
           freigegeben werden mÅûten!
         !end_itemize

 19.01.2001 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item (!I)ACSpro(!i) hatte bislang die Nachricht WM_UNTOPPED nicht
           verarbeitet. Jetzt wird sie "gezielt" ignoriert... ;-)

         !item Die Nachricht WM_TOOLBAR wird nun von (!I)ACSpro(!i) ebenfalls
           "gezielt" ignoriert. SpÑter, wenn ich herausgefunden habe,
           wann diese Nachricht wozu verschickt wird, soll sie jedoch
           passend ausgewertet werden.
         !end_itemize

 18.01.2001 (!link [ACSPLUS.LIB] [C-Erweiterungs-Bibliothek])

         !begin_itemize
         !item Die Titelzeile des Module-Fensters kann nun geÑndert
           werden. Dazu muû in ACSinit0 - oder der entsprechenden
           Routine - der Name des Desktops geÑndert werden, in C z.B.
           durch PUR_DESK.name = " <Neuer Name> ". Das Module-Fenster
           Åbernimmt diesen Namen, sobald dieser vom Standard-Namen
           abweicht.

         !item Die Toolbar des Module-Fensters wurde etwas zu klein, wenn
           der Systemzeichensatz zu klein eingestellt war. Jetzt ist
           die Grîûe der Toolbar von der Grîûe des Systemzeichensatzes
           unabhÑngig.
         !end_itemize

 08.01.2001 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Das Thermometer Ash_thermometer ist Åberarbeitet worden

             !begin_enumerate
             !item Die Optik ist Åberarbeitet worden - der Laufbalken ist
                nicht mehr so "dick", was sich nicht nur bei der
                besseren proportionerten Optik, sondern auch beim
                Neuzeichnen bemerkbar macht.

             !item Das von Ash_thermometer zur VerfÅgung gestellt
                Schleifen-Konstrukt wurde ausgebaut. Die Schleife
                heiût jetzt prinzipiell:

!begin_sourcecode
init(data);
if( start(data) )
{
  while( cont(data) )
    fkt(data);
  stop(data);
}
quit(data);
!end_sourcecode

                öber den neu zu Åbergebenden Funktionszeiger cont wird
                die Abbruchbedingung - bzw. korrekter die
                "Weiterlauf-Bedingung" realisiert. Dies hat zwei
                Auswirkungen. Die hier Åbergebene Funktion kann ebenso
                in stop genutzt werden, um die Unterscheidung
                "vorzeitiger Abbruch" oder "Ende" zu unterscheiden
                (siehe Ash_thermometer). Ferner wird der evtl.
                anzuzeigende Text durch die in cont Åbergebene
                Funktion definiert, er wird also bei der eigentlichn
                Aktion (in fkt Åbergeben) bereits angezeigt!

                (!B)Hinweis:(!b) Falls in cont der Wert NULL bzw. NIL
                Åbergeben wird, wird die Schleife nicht ausgefÅhrt.

             !item Die Struktur ThermoData wurde in A_ThermoData
                umbenannt. Ferner wurde sie um die an Ash_thermometer
                Åbergebenen Funktionszeiger erweitert - hiermit sind
                also z.B. generische "Stop-Nachfrage-Routinen"
                mîglich, die anhand des dort Åbergebenen Zeigers die
                oben erwÑhnte Unterscheidung treffen.
             !end_enumerate
         !end_itemize

 08.01.2001 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Die Nachricht FNT_CHANGED wird unterstÅtzt. Alle
           ZeichensÑtze werden mittels vst_unload_fonts "weggeworfen"
           und mittels vst_load_fonts neu geladen. Dann wird allen
           Fenstern die Nachricht AS_FONT_UPDATE gesendet.

         !item Die Nachricht PRN_CHANGED wird unterstÅtzt. Allen Fenstern
           wird die Nachricht AS_PRN_UPDATE gesendet. In der Åber
           in_out mitgegebenen Struktur A_PrnUpd stehen die
           Informationen Åber den treiber und die Art der énderung.
         !end_itemize

 01.01.2001 (!I)GUI-Editor(!i)

         !begin_itemize
         !item Anpassung der Jahreszahl-Daten im Copyright.

         !item Erstellung des Releases 2.3.4.
         !end_itemize

 30.12.2000 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item (!I)ACSpro(!i) unterstÅtzt nun die WDialog-Druckdialoge. Die
           Funktion A_print stellt die Schnittstelle dazu dar.
           öbergeben werden die Drucker-Einstellungen (siehe WDialog-
           Dokumentation), die Position des Fensters, die zwei Flags
           flag3D (siehe pdlg_create) und option (siehe pdlg_open),
           der Titel des Fensters sowie ganz analog zu Af_fileselect
           das Fenster, dem bei Erfolg die Nachricht AS_PRINT gesendet
           werden soll. Der Dialog erscheint als nicht modaler-Dialog,
           wenn das Fenster angegeben wird, und als modaler Fenster-
           Dialog (sofern mîglich ;-), wenn als Fenster NULL bzw. NIL
           Åbergeben wird.

           (!B)Hinweis:(!b) Allerdings ist hier noch ein Bug fÅr den nicht-
           modalen Betrieb, aber zunÑchst kann einmal damit
           experimentiert werden. Der Bug ist etwas merkwÅrdig, die
           Anwendung stÅrzt gnadenlos ab, da der AES-Aufruf KÑse
           macht. Warum das aber nur im nicht-modalen Betrieb passiert
           und mit modalen Fenster-Dialogen klappt, muû ich noch
           herausbekommen...

           (!B)Hinweis:(!b) Eine Erweiterung des Druck-Dialoges durch eigene
           ObjektbÑume ist in der Planung und wird nach der Version
           3.0.0 realisiert. Wichtig war erst einmal, daû der
           Druckdialog Åberhaupt unterstÅtzt wird... ;-)
         !end_itemize

 22.12.2000 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Erste Vorbereitungen, um auch das im Entstehen begriffene
           SSP (System Service Protocol - siehe im Internet unter
           http://www.therapy-seriouz.de/ssp/) transparent in (!I)ACSpro(!i)
           einzubinden.
         !end_itemize

 12.12.2000 ACSMOD.LIB, ACSMOD01.LIB

         !begin_itemize
         !item Die neuen Funktionen Ast_ncmp und Ast_incmp stehen nun auch
           in der Modulschnittstelle zur VerfÅgung.
         !end_itemize

 12.12.2000 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Es gibt zwei neue Funktionen fÅr String-Vergleiche. Die
           Funktion Ast_ncmp vergleicht zwei Strings, jedoch maximal n
           Zeichen, bei Ast_incmp spielt dabei die Groû-/
           Kleinschreibung keine Rolle.

         !item Im Standard-AboutMe-Dialog von (!I)ACSpro(!i) wird nun das Datum
           der Version "ordentlich" als "tt.mm.jjjj" (deutsch) bzw.
           "mm-tt-jjjj"(englisch) dargestellt. Die "elende KrÅcke" mit
           der AbkÅrzung des Monatsnamens als "Oct" oder "Dec" hat
           also ein Ende... ;-)
         !end_itemize

 11.12.2000 ACSMOD.LIB, ACSMOD01.LIB

         !begin_itemize
         !item Die Funktion appl_xgetinfo steht nun auch Åber die Modul-
           Schnittstelle allen Modulen zur VerfÅgung.

         !item Die Funktion Awi_ontop fehlte in der Modul-Schnittstelle
           und ist nachgerÅstet worden.
         !end_itemize

 03.12.2000 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Der letzte Parameter von Af_fileselect wurde geÑndert: es
           wird nun nicht mehr TRUE oder FALSE als Kennung modal/
           nicht-modal Åbergeben, sondern das Fenster, daû die Antwort
           AS_FILESELECT im nicht-modalen Fall erhalten soll, bzw.
           NULL/NIL fÅr den modalen File-Selector.
         !end_itemize

 02.11.2000 (!I)GUI-Editor(!i)

         !begin_itemize
         !item Bei der direkten Positionseingabe fÅr ein Objekt im
           Objekt-Editor konnten zu groûe oder zu kleine Werte fÅr die
           Zeichen- und Pixel-Komponenten eingegeben werden. Die
           Zeichenkoordinaten sind auf 0..255, die Pixel-Korrekturen
           auf -128..127 festgelegt. Grîûere oder kleinere Werte
           werden passend auf den Rand des Intervalls gesetzt.
         !end_itemize

 30.10.2000 PCGEM.LIB, ACSGEM.LIB

         !begin_itemize
         !item Die GEM-Bibliotheken sind nun aus (!I)ACSpro(!i) ausgegliedert
           worden und laufen ab jetzt als eigenstÑndige Projekte. Sie
           erhalten eine eigene History und entfallen ab jetzt in
           dieser Liste komplett.

           Die GEM-Bibliotheken sind Freeware und wie (!I)ACSpro(!i) auch in
           der Maus LL und auf dem Internet verfÅgbar.

           Alle "Bestandteile" von (!I)ACSpro(!i) ab Version 3.0.0 sind ab
           jetzt immer mit der jeweils neuesten Bibliothek PCGEM.LIB
           compiliert bzw. gelinkt.
         !end_itemize

 26.10.2000 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Die Nachricht WM_MOUSEWHEEL wird von (!I)ACSpro(!i) nun empfangen,
           allerdings noch nicht groûartig ausgewertet. Dies ist ein
           einer spÑteren Version allerdings geplant (siehe ToDo-
           Liste).
         !end_itemize

 26.10.2000 PCGEM.LIB, ACSGEM.LIB

         !begin_itemize
         !item Die Nachricht WM_MOUSEWHEEL ist nun in die Liste der
           Nachrichten mit aufgenommen worden.
         !end_itemize

 25.10.2000 PCTOS.LIB, ACSTOS.LIB

         !begin_itemize
         !item Die TOS-Bibliotheken sind nun aus (!I)ACSpro(!i) ausgegliedert
           worden und laufen ab jetzt als eigenstÑndige Projekte. Sie
           erhalten eine eigene History und entfallen ab jetzt in
           dieser Liste komplett.

           Die TOS-Bibliotheken sind Freeware und wie (!I)ACSpro(!i) auch in
           der Maus LL und auf dem Internet verfÅgbar.

           Alle "Bestandteile" von (!I)ACSpro(!i) ab Version 3.0.0 sind ab
           jetzt immer mit der jeweils neuesten Bibliothek ACSTOS.LIB
           compiliert bzw. gelinkt.
         !end_itemize

 19.10.2000 PCGEM.LIB, ACSGEM.LIB

         !begin_itemize
         !item Aus der N.AES-Entwickler-Dokumentation sind einige N.AES-
           spezifische Funktionen in die Bibliotheken eingeflossen,
           wie z.B. appl_control, wind_draw, shel_help, objc_xfind
           etc.
         !end_itemize

 18.10.2000 (!link [ACSPLUS.LIB] [C-Erweiterungs-Bibliothek])

         !begin_itemize
         !item Der "pure" Desktop reagiert nun auf die Nachricht
           AS_DRAGGED, indem die Auswahlliste nach Objekten vom Typ
           AT_FILE durchsucht wird. Wenn diese gefunden werden und
           eine Neu-Routine hinterlegt wurde, wird fÅr jedes AT_FILE-
           Objekt die Neu-Routine aufgerufen.

           (!B)Hinweis:(!b) Damit kann z.B. jede (!I)ACSpro(!i)-Anwendung, die den
           PUR_DESK verwendet, - wie z.B. der (!I)GUI-Editor(!i) - per
           Drag&Drop von jinnee Dateien empfangen und îffnen (sofern
           dies in jinnee entsprechend eingestellt ist natÅrlich ;-)
         !end_itemize

 18.10.2000 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Die Behandlung des Drag&Drop-Protokolles fÅr Text-Daten
           wurde verbessert. Wenn eine laufende (!I)ACSpro(!i)-Anwendung Daten
           per Drag&Drop gesendet bekommt (z.B. von jinnee bei einem
           Mehrfachstart - muû in jinnee eingestellt werden), so
           werden der Reihe nach die einzelnen Text-Daten in Objekte
           vom Typ AT_FILE (D&D-Typ ARGS) oder AT_STRING (sonst)
           "gesteckt" und an eine (!link [Drag-Routine] [Click-Routine]) gesendet.

           Bislang wurden empfangene Text-Daten Åber die
           Awindow.keys-Routine eingespielt, wenn das Empfangsobjekt
           nicht identifiziert werden konnte. Dies ist nicht immer das
           GewÅnschte, denn da in dieser Situation auch kein
           Empfangsfenster bekannt ist, verwendet (!I)ACSpro(!i) automagisch
           das Root-Fenster und ÅbertrÑgt die Dateinamen per
           Awindow.keys-Routine... ;-/

           Wenn jetzt das Objekt nicht bestimmt werden kann, wird dem
           Empfangsfenster - nach erfolgreicher PrÅfung durch die
           Nachricht AS_CHECKDRAGGED - die Nachricht AS_DRAGGED
           gesendet, als wenn das D&D-Objekt aus der Auswahlliste auf
           das Fenster-Icon gezogen worden wÑren. Das Fenster kann nun
           geeignet darauf reagieren.

           (!B)Hinweis:(!b) Falls das Empfangsobjekt lokalisiert werden kann,
           aber kein erweitertes AOBJECT ist oder keine (!link [Drag-Routine] [Click-Routine])
           besitzt, bleibt alles so wie bisher: die "Text-Daten"
           werden per Awindow.keys-Routine eingespielt.
         !end_itemize

 16.10.2000 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Nachdem ich selbst beim Entwickeln von und mit (!I)ACSpro(!i) immer
           wieder in die Situation komme, wo es heiût "jetzt muû ich
           nur noch schnell eine Nachricht an das Objekt xyz schicken"
           und dann der Zirkus mit dem Bestimmen der AUSERBLK-Struktur
           und dem Versand der Nachricht losgeht, gibt es jetzt
           (endlich ;-) zwei neue Funktionen:

             !begin_enumerate
             !item Die Funktion Aob_service leitet eine Nachricht an ein
                Objekt eines Objektbaumes weiter. Wenn das (globale)
                Flag AB_LAZYEVAL gesetzt ist, werden keine
                öberprÅfungen mehr vorgenommen. Andernfalls wird
                geprÅft, daû der Objektbaum vorhanden ist, daû es ein
                UserDef ist und eine Service-Routine vorhanden ist...

             !item Die Funktion Awi_observice leitet eine Nachricht an
                ein Fenster-Objekt weiter. Auch hier wird nur bei
                gelîschtem Flag AB_LAZYEVAL geprÅft, daû das Fenster
                existiert. Die Funktion selbst ist recht billig, denn
                sie lÑût - mit dem passenden Objektbaum - die Arbeit
                von Aob_service erledigen... ;-)
             !end_enumerate
         !end_itemize

 15.10.2000 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Wenn der Dateiname bei der Funktion Af_fileselect nur aus
           dem String "*" besteht, so wird der Dateiname fÅr den
           FSLX-File-Selector (!link [A_fileselect] [Ash_fileselect]) geleert - sieht einfach
           besser aus als ein Dateiname "*"... ;-)

         !item Die Funktion Aob_state wurde etwas Åberarbeitet, sie hat
           nicht immer das getan, was sie sollte. Das Ergebnis war von
           der Uhrzeit, der Wetterlage und der Luftfeuchtigkeit
           abhÑngig - na ja, sooo schlimm war es nicht, aber je
           nachdem, ob das Fenster geîffnet war oder nicht, ob der
           Zustand gesetzt werden sollte oder gelîscht, gab es hin und
           wieder unterschiedliche Ergebnisse... Jetzt sollte es
           funktionieren...

         !item Die Nachricht AS_FILESELECT war leider falsch definiert und
           hat sich mit AS_ALLOWBUBBLE Åberschnitten. Damit
           funktionierte BubbleGEM nicht mehr, wenn die Nachricht
           AS_FILESELECT von der Service-Routine bearbeitet wurde...
           AS_FILESELECT hat nun eine neue Nummer bekommen... Sorry!
         !end_itemize

 15.10.2000 ACS_LOAD.O

         !begin_itemize
         !item Die Funktion Af_fileselect war im Modul-Startup-Code
           fehlerhaft implementiert und hat im (!I)ACSpro(!i)-Hauptprogramm
           die falsche Funktion aufgerufen - - Folge: Crash... ;-/
         !end_itemize

 13.10.2000 (!link [ACSPLUS.LIB] [C-Erweiterungs-Bibliothek]), (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Die Funktion Ash_thermometer ist nun aus der Bibliothek
           (!link [ACSPLUS.LIB] [C-Erweiterungs-Bibliothek]) in die Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek]) gewandert, da sie
           dort eher paût als in die "ErgÑnzungsbibliothek".
         !end_itemize

 12.10.2000 ACS_LOAD.O

         !begin_itemize
         !item Der Startup-Code fÅr (!I)ACSpro(!i)-Module wurde um die neuen
           Funktionen Af_fileselect, (!link [A_fileselect] [Ash_fileselect]) und Ash_thermometer
           erweitert, so daû auch Module diese Funktionen nutzen
           kînnen, ohne daû (!link [ACS.LIB] [Pure-C-Bibliothek]) gelinkt werden muû.
         !end_itemize

 08.10.2000 (!I)GUI-Editor(!i)

         !begin_itemize
         !item Nachdem ich in KÅrze umziehen werde, ist nun im (!I)GUI-Editor(!i)
           beim Info-Dialog, der auf die (!link [Shareware] [Shareware])-EinschÑnkung
           hinweist, bereits meine neue Adresse enthalten - so wie auf
           den Internetseiten und in der ST-Guide-Hilfe auch... ;-)
         !end_itemize

 06.10.2000 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Der nicht-modale File-Selector hatte noch einen Bug, durch
           den u.U. auf bereits freigegebenen Speicher zugegriffen
           wurde.
         !end_itemize

 25.09.2000 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Bei der Umwandlung der MenÅs in das "moderne Layout" war
           noch ein kleiner Fehler enthalten, so daû unter UmstÑnden
           nicht angeforderte Strings freigegeben wurden.
         !end_itemize

 25.09.2000 (!link [ACSPLUS.LIB] [C-Erweiterungs-Bibliothek])

         !begin_itemize
         !item Die neue Funktion Ash_thermometer stellt ein sog.
           (!I)Thermometer(!i) (Woher der Name dafÅr stammt, weiû ich gar
           nicht mehr - ich nenne es schon "ewig" so...) zur
           VerfÅgung. Ich habe diese Funktion nach etwas Zîgern aus
           meiner langjÑhrigen "(!I)ACSpro(!i)-Bibliothek-Sammlung" geholt,
           sie etwas verallgemeinert und Åberarbeitet, damit sie in
           der Bibliothek (!link [ACSPLUS.LIB] [C-Erweiterungs-Bibliothek]) nun allen zur VerfÅgung steht.

           Die Funktion Ash_thermometer stellt prinzipiell ein
           Programm der Form

!begin_sourcecode
init(data);
if( start(data) )
{
   while( fkt(data) );
   stop(data);
}
quit(data);
!end_sourcecode

           dar. Der Arbeitsfortschritt der Schleifen-Funktion fkt wird
           durch einen Laufbalken mit zugehîriger Prozentzahl in einem
           modalen Fenster-Dialog darstellt. Falls das Fenster fÅr den
           Fenster-Dialog nicht erstellt werden kann, lÑuft fast die
           dargestellte Schleifenkonstruktion ohne optische
           RÅckmeldung durch.

           Die in dieser Schleifenkonstruktrion dargestellten
           Funktionen werden als Funktionszeiger, die Åbrigens auch
           NULL sein dÅrfen, an Ash_thermometer Åbergeben:

!begin_sourcecode
Ash_thermometer(
   THERMO_CANCEL|THERMO_TEXT|THERMO_PERCENT,
   " Ein kleines Beispiel... ", color, data, init,
   start, zaehlfkt, stop, quit);
!end_sourcecode

           Das Thermometer gibt es in insgesamt 4 Varianten. Das
           Fenster kann einen Info-Text anzeigen, oder der Platz dafÅr
           wird eingespart. Ferner kann die Aktion abgebrochen werden,
           oder es ist kein Abbrechen-Button vorhanden. Die in der
           Mitte des Laufbalkens plazierte Prozentzahl kann bei jeder
           der Varianten ausgeblendet werden.
         !end_itemize

 22.09.2000 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Die bisher intern genutzte Funktion appl_xgetinfo wurde
           Åberarbeitet und steht nun "offiziell" zur VerfÅgung. Die
           Funktion prÅft, ob appl_getinfo verfÅgbar ist und ruft
           diese ggf. auf.
         !end_itemize

 22.09.2000 (!I)GUI-Editor(!i)

         !begin_itemize
         !item Der (!I)GUI-Editor(!i) verwendet nun die neue Funktion zur
           Dateiauswahl. Die Routine zum ôffnen der ACS-Dateien wurde
           Åberarbeitet und auf die neue Dateiauswahl angepaût. Zum
           ôffnen von ACS-Dateien wurde der modale Fileselector mit
           Multi-Select-FÑhigkeiten gewÑhlt.

           FÅr Interessierte:
           Das Code-Fragment im (!I)GUI-Editor(!i) lautet

!begin_sourcecode
liste=Af_fileselect(ttl_open, last_path, "acs",
   TRUE, FALSE);
if( liste!=NULL )
{
   (liste->doFor)(liste, &window, NO_FOR,
     (A_ListWork)openFile);
   Alu_delete(liste);
}
!end_sourcecode

           Der letzte Parameter von Af_fileselect gibt an, ob der
           Dialog modal sein soll, der vorletzte steuert die Multi-
           Selektion.
         !end_itemize

 22.09.2000 (!link [ACSPLUS.LIB] [C-Erweiterungs-Bibliothek])

         !begin_itemize
         !item Der Desktop PUR_DESK versteht nun die neue Nachricht
           AS_FILESELECT, die von dem nicht-modalen Fileselector
           verschickt wird. Wenn eine Neu-Routine angemeldet wurde, so
           werden dieser der Reihe nach die Dateinamen zum ôffnen
           Åbergeben.
         !end_itemize

 21.09.2000 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Die neue Funktion Ash_fileselect îffnet den
           (MagiC-)Fileselector. Je nach Parametern kann der
           Fileselector als modales oder auch nicht-modales Fenster
           geîffnet werden. Wenn der Fileselector als nicht-modales
           Fenster lÑuft, wird beim Schlieûen des Selektors dem beim
           Aufruf mitgegebenen Fenster die Nachricht AS_FILESELECT
           zusammen mit einer linearen Liste ULinList an Pfadnamen
           geschickt (das Freigeben der Liste Åbernimmt hier (!I)ACSpro(!i)).

           Na ja, es ist nicht unbedingt der MagiC-Fileselector, es
           ist der Fileselector, der Åber die FSLX-Funktionen bedient
           wird... ;-) Daraus folgt aber auch: ohne FSLX-Funktionen
           (Abfrage erfolgt per appl_xgetinfo) gibt es hier nichts!

         !item Um bei allen Varianten der Dateiauswahl immer ein Optimum
           zu erzielen, gibt es nun die Funktion Af_fileselect, die
           direkt eine lineare Liste ULinList von Dateinamen
           zurÅckliefert - wie daher zu erwarten ist, wird eine
           Mehrfach-Selektion unterstÅtzt. Die Funktion greift auf die
           Funktion Ash_fileselect zurÅck, sofern die FSLX-Funktionen
           vorhanden sind. Andernfalls wird die Kombination
           Af_first_fsel/Af_next_fsel (Multi-Selektion) bzw. Af_select
           (Single-Selektion) verwendet. Af_fileselect erlaubt auch
           den Aufruf des nicht-modalen Fileselectors. In diesem Falle
           wird die Nachricht AS_FILESELECT an das Root-Fenster
           gesendet.

           (!B)Hinweis:(!b) Die Verwendung der Funktionen Af_select,
           Af_first_fsel und Af_next_fsel sollte mîglichst zugunsten
           der neuen Funktion Af_fileselect aufgegeben werden.
         !end_itemize

 17.09.2000 (!I)GUI-Editor(!i)

         !begin_itemize
         !item Der (!I)GUI-Editor(!i) kam nicht so recht mit langen Dateinamen
           klar. Besonders problematisch waren Dateinamen mit
           Leerzeichen darin. Der (!I)GUI-Editor(!i) versteht nun korrekt
           gequote Dateinamen, sowohl beim Start in der Kommandozeile
           als auch beim AV-(!link [Protokoll] [Protokoll]) per VA_START.

         !item Die Dateinamen wurden stets in Groûbuchstaben umgewandelt.
           Das ist etwas lÑstig, besonders bei casesensitiven
           Filesystemen. Die Dateinamen werden nun nicht mehr
           umgewandelt, sondern bleiben so, wie sie sind - sowohl beim
           ôffnen von Dateien als auch beim Neuanlegen von Dateien.

         !item Beim Start werden Åbergebene Optionen der Form "-..." bzw.
           "/.." erkannt, wobei nach den Zeichen '-' bzw. '/' beliebig
           viele Zeichen folgen kînnen. Bisher wurde versucht, diesen
           Parameter als Datei zu îffnen. Nachdem der (!I)GUI-Editor(!i)
           (noch;-) keine Optionen kennt, werden diese Argumente
           schlichtweg ignoriert.
         !end_itemize

 15.09.2000 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Die Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek]) enthÑlt nun auch die beiden
           Bibliotheken ACSTOS.LIB und PCGEM.LIB. Das Einbinden der
           Bibliotheken PCTOS.LIB, PCTOSLIB.LIB, PCGEM.LIB oder
           PCGEMLIB.LIB entfÑllt somit vîllig und ist nicht mehr
           notwendig.

           Bisher muûte die Bibliothek ACSTOS.LIB im Projekt enthalten
           sein. Falls dies vergessen wurde und eine der Bibliotheken
           PCTOS.LIB oder PCTOSLIB.LIB (Pure-C-Biblitohek) verwendet
           wurde, kam es zu AbstÅrzen, da (!I)ACSpro(!i) ab Version 3.0.0
           bereits die optimierten Aufrufe aus ACSTOS.LIB verwendet.
         !end_itemize

 05.09.2000 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Die Flags in der Struktur Adescr wurde um AB_OLDMENU
           erweitert.

         !item Die MenÅs kînnen - falls das Flag AB_OLDMENU nicht gesetzt
           ist - in das "moderne Layout" umgewandelt werden, so daû
           die MenÅs dreidimensional erscheinen. Folgende Umwandlungen
           werden vorgenommen:

             !begin_enumerate
             !item Alle G_BOX-Objekte werden bei mindestens 16 Farben mit
                Hintergrund LWHITE und FÅllmuster IP_SOLID versehen.
                Der 3D-Effekt wird durch das Flag FL3DBAK in ob_flags
                bewirkt.

                Diese Effekte wirken also nur auf Systemen, die die
                AES-4-Objekt-Flags kennen...

             !item Alle Strings G_STRING, die disabled sind und nur das
                Zeichen '-' enthalten, werden in das Userdef-Arrows
                umgewandelt. Bei mindestens 16 Farben wird die Linie
                in der Farbe LBLACK mit der StÑrke 1, andernfalls in
                der Farbe BLACK mit StÑrke 3 gezeichnet.

             !item Alle Arrow-Userdefs, die disabled sind und in den
                AUSERBLK-Strukturen die Default-Werte des Userdef-
                Arrows in MenÅs enthalten, werden bei mindestens 16
                Farben umgesetzt auf Linienfarbe LBLACK und StÑrke 1.
             !end_enumerate

         !item Das Userdef-Arrows verhÑlt sich nun beim Status DISABLED
           etwas anders. Wenn die StÑrke 3 verwendet wird, wird das
           Userdef wie bisher gezeichnet (die Zeichenroutine
           bearbeitet nur den Status SELECTED). Falls jedoch die
           StÑrke 1 verwendet wird, wird zusÑtzlich der Status
           DISABLED durch die Zeichen-Routine bearbeitet. Damit sieht
           das Userdef als Trennlinie bei MenÅs besser aus... ;-)

           (!B)Hinweis:(!b) Der Status SELECTED bzw. DISABLED hat jedoch keine
           Auswirkung auf das Aussehen des Userdefs!
         !end_itemize

 03.09.2000 ACSGEM.LIB, PCGEM.LIB

         !begin_itemize
         !item Das VDI-Binding wurde Åberarbeitet. Die auf dem Stack
           angelegten öbergabe-Arrays sind nun etwas grîûer
           dimensioniert - evtl. waren sie einfach zu klein, so daû es
           beim Aufruf "krachte".
         !end_itemize

 03.09.2000 ACS.PAS, ACS.PPU

         !begin_itemize
         !item Bei der Pure-Pascal-Unit wurde kein Environment an (!I)ACSpro(!i)
           Åbergeben. Damit lieferte die Funktion Ash_getenv natÅrlich
           nur "KÑse" ;-) Dies ist jetzt korrigiert, so daû nun alle
           Environment-Variablen unter Pure-Pascal zur VerfÅgung
           stehen...
         !end_itemize

 29.07.2000 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Die öberprÅfungen des Speichers beim Anfordern und
           freigeben per Ax_malloc, Ax_free bzw. Ax_ifree wurden
           komplett Åberarbeitet. Wenn das Flag AB_NOMEMCHECK gesetzt
           ist, findet jetzt keinerlei PrÅfung mehr statt. Das
           Beschreiben des Speichers vor dem von Ax_malloc gelieferten
           Blockes mit einer (!I)ACSpro(!i)-eigenen Kennung ist nun zusammen
           mit der zugehîrigen öberprÅfung der Kennung in Ax_free bzw.
           Ax_ifree auch weggefallen - bei mir hat dies nicht korrekt
           funktioniert, nach mehreren Crashes von MagiC habe ich es
           herausgeworfen, denn MagiC macht intern genau so etwas
           auch... ;-)

           Wenn diese Art der Speicher-öberprÅfung denoch benîtigt
           wird, dann werde ich eben wieder so eine nette Sache
           einbauen... ;-)

         !item Die Funktion nkc_gconv wurde in nkc_gem2n, die Funktion
           nkc_tconv in nkc_tos2n umbenannt. Damit alte Sourcen nicht
           angefaût werden mÅssen, werden Åber Makros die alten in die
           neuen Namen Åbersetzt.

         !item (!I)ACSpro(!i) verwendet nun nicht mehr direkt die NKCC-Routinen,
           sondern die von Christian Felsch Åberarbeitete Version aus
           der CF-Lib.

           Da dort die Funktionen nkc_n2gem, nkc_gem2n, nkc_cmp und
           nkc_kstate nicht enthalten sind, wurden diese in C
           entsprechend nachprogrammiert.

         !item Neu dazu gekommen sind die Funktionen nkc_gemks2n,
           nkc_n2gemks und nkc_n2kstate.
         !end_itemize

 12.07.2000 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Die Modul-Schnittstelle wurde nun komplett Åberarbeitet. In
           Zukunft sollten also abwÑrtskompatible Erweiterungen
           wesentlich leichter zu realisieren sein.

           Da es bestimmt einige Module gibt, die nicht mehr
           weitergepflegt werden, habe ich mich entschlossen, hier zu
           tricksen und etwas mehr Aufwand zu treiben: wird eine alte
           Modul-Schnittstelle erkannt (also von (!I)ACSpro(!i) 2.3x bzw.
           2.3.x), so werden die Daten vor dem Weiterreichen an die
           Anwendung konvertiert. Damit kînnen auch Module von (!I)ACSpro(!i)
           2.3x/2.3.x geladen werden und scheinen zu funktionieren -
           jedenfalls soweit ich sie habe und testen kann...

           Da das nicht jeder benîtigt, gibt es nun drei Bibliotheken,
           von denen genau eine zusÑtzlich zur Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek]) in
           Projekte eingebunden werden muû:

            !begin_xlist [ACSMODNO.LIB]
            !item [ACSMODNO.LIB]  Die Funktion Ash_module existiert nur als
                          Dummy-Funktion. Module werden nicht geladen,
                          eine Schnittstelle wird nicht zur VerfÅgung
                          gestellt. Durch Verwendung dieser Bibliothek
                          werden nun von der Anwendung nicht benîtigte
                          Routinen vom Linker nicht mehr eingebunden,
                          die andernfalls nur wegen der Modul-
                          Schnittstelle denoch hinzugelinkt wÅrden..

            !item [ACSMOD01.LIB]  Die Modul-Schnittstelle Version 1 (neue
                          Versionierung der Schnittstelle unabhÑngig
                          von der (!I)ACSpro(!i)-Version) wird zur VerfÅgung
                          gestellt. Die Funktion Ash_module lÑdt nur
                          die neuen Module nach, die mit dieser
                          Schnittstellen-Version erstellt wurden.

            !item [ACSMOD.LIB]    Die Modul-Schnittstelle steht in
                          abwÑrtskompatibler Form zur VerfÅgung. Es
                          werden auch die Module der (!I)ACSpro(!i)-Versionen
                          2.30, 2.31, 2.32, 2.3.3 und 2.3.4 geladen
                          und gestartet. In der Schnittstelle stehen
                          fÅr die Erweiterungen der Strukturen
                          Awindows und AUSERBLK entsprechende
                          Konvertierungsroutinen bereit, die vîllig
                          unbemerkt fÅr die KompatibilitÑt sorgen.
            !end_xlist
         !end_itemize

 09.07.2000 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Die Modul-Schnittstelle ist nun in eine eigenstÑndige
           Bibliothek ausgelagert worden. Die neue Bibliothek
           ACSMOD01.LIB muû zusÑtzlich zur Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek]) in
           Projekte eingebunden werden.

           FÅr Anwendungen, die keine Modulschnittstelle benîtigen,
           existiert die Bibliothek ACSMODNO.LIB, die eine leere
           Schnittstelle enthÑlt. Damit bindet der Linker auch nur die
           wirklich benîtigten (!I)ACSpro(!i)-Funktionen ein.

           Dank an Rainer Mannigel fÅr diese Anregung!
         !end_itemize

 08.07.2000 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Anwendungen, die mit (!I)ACSpro(!i) entwickelt werden, brauchen nun
           nicht mehr die Line-A-Bibliothek von Pure-C - auûer wenn
           der Entwickler selbst irgendetwas davon benîtigt, wovon
           allerdings (!I)dringendst(!i) abgeraten wird. (!I)ACSpro(!i) verwendet hier
           nur die sog. "Initialisierung" ($A000) zur Ermittelung der
           Line-A-Variablen. Diese werden nun Åber eine eigene "Mini-
           Funktion" beschafft.

           Benîtigt werden die Line-A-Variablen nur in Abp_start und
           Abp_end. Diese beiden Routinen sollten sowie so nur
           verwendet werden, wenn alle anderen Stricke reiûen...
           (siehe auch die Dokumentation der beiden Funktionen ;-)
         !end_itemize

 06.07.2000 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Interne AufrÑumarbeiten in Af_select und Af_first_fsel...
         !end_itemize

 02.07.2000 (!I)GUI-Editor(!i)

         !begin_itemize
         !item Der (!I)GUI-Editor(!i) verwendet nun den Startup-Code von Stefan
           Kuhr @ OS. Vielen Dank, Stefan! Bisher wurde der Startup-
           Code von Behne&Behne verwendet.

           Beide Startup-Codes sind an PowerMacs unter (!link [MagiCMac] [Mag!X])
           angepaût und liegen (!I)ACSpro(!i) bei. Die Original-Archive der
           Startup-Codes sind u.a. in der Maus LL im
           Gruppenprogrammteil Maus.Computer.Atari.Programmieren zu
           finden.

           Hier ein kleiner Vergleich (unvollstÑndig und natÅrlich
           subjektiv!) der beiden Startup-Codes:

           Der Startup-Code von Stefan Kuhr unterstÅtzt:

              !begin_itemize
              !item Quoting (Kommandozeilenparameter mit Leerzeichen)

              !item ARGV (Kommandozeilenparameter mit mehr als 125 Zeichen
                werden im Environment Åbergeben)

              !item erweitertes ARGV (öbergabe von leeren Parametern)

              !item Workaround fÅr PATH

              !item Shared Text (Programm mehrfach starten, dessen Text-
                Segment aber nur einmal im Speicher liegt)

              !item keine FPU-Initialisierung
              !end_itemize

           Der Startup-Code von Behne&Behne unterstÅtzt:

              !begin_itemize
              !item Quoting (Kommandozeilenparameter mit Leerzeichen)

              !item ARGV (Kommandozeilenparameter mit mehr als 125 Zeichen
                werden im Environment Åbergeben)

              !item keine FPU-Initialisierung
              !end_itemize
         !end_itemize

 26.06.2000 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Die Funktion Ax_memCheck prÅft, ob ein Speicherbereich les-
           und/oder beschreibbar ist. Da bei den diversen Protokollen
           diese Funktion verwendet wird, sollten (!I)ACSpro(!i) und alle
           mittels (!I)ACSpro(!i) entwickelten Anwendungen nicht mehr
           abstÅrzen, wenn der Kommunikationspartner bei
           eingeschaltetem Speicherschutz den "Transfer-Speicher"
           nicht global alloziert hat.

           Dank an Thomas Binder fÅr die geniale Routine im MausNet in
           der Gruppe Maus.Computer.Atari.Programmieren!
         !end_itemize

 26.06.2000 ACSGEM.LIB, PCGEM.LIB

         !begin_itemize
         !item Fehler bei appl_search korrigiert, die RÅckgabewerte wurden
           nicht ordentlich gesetzt. Damit klappte die Kommunikation
           per XAcc-(!link [Protokoll] [Protokoll]) etc. nicht mehr... ;-/

         !item Die PDLG-Routinen von WDialog wurden nun ebenfalls
           implementiert.
         !end_itemize

 25.06.2000 ACSGEM.LIB, PCGEM.LIB

         !begin_itemize
         !item In die Bibliothek wurden die LBOX-Funktionen fÅr WDialog
           aufgenommen.
         !end_itemize

 23.06.2000 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Neue Funktionen, die fÅr den Umgang mit NVDI nÅtzlich sind,
           wurden implementiert. Es handelt sich um Ash_NVDICookie,
           Ash_CallEdDI, Ash_NVDIVersion und Ash_NVDIDatum.
         !end_itemize

 23.06.2000 ACSGEM.LIB, PCGEM.LIB

         !begin_itemize
         !item Die Bibliothek wurde noch etwas Åberarbeitet. Die EdDI-
           Funktionen v_opnbm, v_clsbm und vq_scrninfo wurden in die
           Bibliothek aufgenommen.

         !item In die Bibliothek wurden die FNTS-Funktionen fÅr WDialog
           aufgenommen.

         !item In die Bibliothek wurden die FSLX-Funktionen fÅr WDialog
           aufgenommen.
         !end_itemize

 22.06.2000 (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Der Fenster-Editor erlaubt nun auch das Setzen des
           Fenster-Elementes HOTCLOSEBOX, das in MagiC ab Version 1.0
           und in KAOS 1.4.2 (auûerdem ab PC-GEM 2) vorhanden ist.
           Damit wird das Schlieûen des Fensters bereits beim
           Anklicken der Schlieûbox und nicht erst beim Loslassen
           ausgelîst.
         !end_itemize

 18.06.2000 (!link [ACS.LIB] [Pure-C-Bibliothek]), (!I)GUI-Editor(!i)

         !begin_itemize
         !item Ash_getcookie verwendet nun Ssystem zum Lesen der (!link [Cookies] [Cookie Jar]) -
           falls die Funktion verfÅgbar ist.

         !item Der Editor des (!link [Cycle] [Cycle])-Button hatte einen kleinen Bug, der
           nun behoben ist. Wenn als "(!link [Cycle] [Cycle])-Zeichen" ein Zeichen
           grîûer 127 gewÑhlt wurde, wurde der Parameter-Wert des
           Userdef falsch gesetzt - d.h. das Vorzeichen des char wurde
           auf den long Åbertragen... ;-)
         !end_itemize

 13.06.2000 PCTOS.LIB

         !begin_itemize
         !item In der Bibliothek PCTOS.LIB hatte sich fÅr Mshrink ein
           Fehler eingeschlichen, der Parameter zero war nicht
           vorhanden. Auswirkungen hatte dies natÅrlich bei allen
           Anwendungen, die diese Bibliothek verwenden, u.a. im (!I)GUI-
           Editor(!i) beim Laden von Modulen.
         !end_itemize

 02.06.2000 (!I)GUI-Editor(!i)

         !begin_itemize
         !item Der (!I)GUI-Editor(!i) hat fÅr Pure-Pascal die Texte des
           Hauptmoduls nicht exportiert (Der Bug war eigentlich immer
           schon so drin, das hat noch nie richtig funktioniert... ;-
           /). Damit stÅrzten Programme ab, wenn per Ash_error oder
           Awi_alert eine Meldung ausgegeben werden sollte, ohne daû
           alert_name gesetzt war.
         !end_itemize

 19.05.2000 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Die Konstanten-Definitionen der MiNTLib fÅr (!link [Cookies] [Cookie Jar]) wurden
           in (!I)ACSpro(!i) Åbernommen. Damit stehen die sprechenderen
           "Namen" der (!link [Cookies] [Cookie Jar]) auch unter GNU-C und Pure-Pascal zur
           VerfÅgung.

         !item Kleineren Bug behoben: Die Funktion Awi_ontop() lieferte
           nach der Nachricht WM_ONTOP fÑlschlicherweise das Root-
           Fenster zurÅck... ;-/
         !end_itemize

 18.05.2000 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek]), (!I)GUI-Editor(!i)

         !begin_itemize
         !item Die Versionsnummern wurden nun umbenannt. Aus V3.00 ist
           V3.0.0 geworden. Damit kann ich auch nun auch mehr als 9
           BugFixes durchfÅhren... ;-)
         !end_itemize

 11.05.2000 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Die unterstÅtzten GEMScript-Kommandos wurden um die
           Kommandos 'KeyPress' und 'GetAllCommands' erweitert. Hier
           wird ein Tastendruck simuliert bzw. eine alphabetisch
           sortierte Liste aller unterstÅtzten GEMScript-Kommandos
           ausgegeben.

           Genaueres ist in der Dokumentation der GEMScript-Funktionen
           Aev_GEMScript und Awi_gemscript zu finden.
         !end_itemize

 11.05.2000 Bibliothek ACSTOS.LIB, PCTOS.LIB

         !begin_itemize
         !item Weitere Fehlerkorrekturen in der TOS-Bibliothek

         !item Die Bibliothek ACSTOS.LIB gibt es nun auch als kompatiblen
           Ersatz der Pure-C Bibliothek. Die neue Bibliothek PCTOS.LIB
           sollte zur bisherigen Pure-C Bibliothek kompatibel sein,
           aber alle mir bekannten BIOS-, XBIOS- und GEMDOS-Funktionen
           enthalten.

         !item Die ACSTOS.LIB gibt es ferner als kompatible Bibliothek,
           die zu 100% kompatibel zur von CAT verwendeten Bibliothek
           ist - eine kleinere "Spielerei", diese Variante, ich gebe
           es zu... ;-)
         !end_itemize

 04.04.2000 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Die Routine Awi_doform warf Bîmbchen, wenn das Erzeugen des
           Fensters nicht funktioniert hat und auf A_dialog
           ausgewichen werden muûte.
         !end_itemize

 12.03.2000 (!I)GUI-Editor(!i)

         !begin_itemize
         !item Im allgemeinen USERDEF-Editor (also Klick mit Ctrl auf
           "Objekt-Editor) gab es einen Absturz, wenn die Strings fÅr
           BubbleGEM und das Context-Popup nicht leer waren.
         !end_itemize

 12.03.2000 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item In Ast_ltrim - und damit auch in Ast_alltrim - war noch ein
           Bug, der das Ende des korrigierten Strings falsch setzte.
         !end_itemize

 05.03.2000 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Die Funktion Awi_obchange funktionierte bei einem Wechsel
           des Objekt-Status von DISABLED auf ENABLED nicht richtig.
           Das Objekt war zwar enabled, wurde aber z.T disabled
           gezeichnet. Meine Untersuchungen haben ein etwas
           merkwÅrdiges Verhalten von objc_change ergeben. Das Problem
           wurde durch einen Workaround umschifft.
         !end_itemize

 13.02.2000 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Im Dialog "Allgemein" des Module-Fensters funktionierte die
           Einstellung "Kopiere Bildteil" nicht richtig. Wenn die
           Option umgestellt wurde, wurde stets das Gegenteil
           durchgefÅhrt. Wenn die Option in der Default-Einstellung
           belassen wurde, klappte alles wie es sollte. Dies ist nun
           korrigiert! Die Einstellungen in diesem Dialog sollten
           daher nochmals kontrolliert werden.
         !end_itemize

 31.01.2000 (!I)GUI-Editor(!i)

         !begin_itemize
         !item Bei Hauptmodulen wurde fÅr Pure-Pascal in der Komponente
           acc_reg ein Char zuviel ausgegeben. Dies ist nun
           korrigiert!
         !end_itemize

 25.01.2000 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Die Funktion Af_readdir - und damit auch Af_first und
           Af_next - funktionierte nicht korrekt, wenn Dxreaddir und
           Dreaddir (MagiC bzw. MiNT) nicht vorhanden sind und intern
           Fsfirst/Fsnext verwendet wurde. Die Dateinamen wurden mit
           "*.*" aufgeblÑht:

                       Laufwerk:\ORDNER1\...\*.*\DATEI.XYZ

           Das Problem ist nun behoben!
         !end_itemize

 05.01.2000 Bibliothek ACSTOS.LIB, PCGEM.LIB, ACSGEM.LIB

         !begin_itemize
         !item Diverse Optimierungen in den Bibliotheken fÅr BIOS, XBIOS,
           GEMDOS, VDI & AES.
         !end_itemize

 23.12.1999 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Das Backdropping wird nun in der Version 3.0.0 noch nicht
           Åber einen Zeiger in der Struktur Awindow aufgerufen,
           sondern noch automatisch und nicht beeinfluûbar. Dies wird
           sich nach der Version 3.0.0 allerdings Ñndern (steht schon
           auf der ToDo-Liste).
         !end_itemize

 16.12.1999 (!I)GUI-Editor(!i)

         !begin_itemize
         !item Die Objekt-Editoren (3D-Button-Dialog, Checkbox-/
           Radiobutton-Dialog, etc.) rufen nun die ST-Guide-Hilfe
           ebenfalls Åber die neue Schnittstelle auf.

         !item Der Objektbaum-Editor hat nun ebenso wie der Popup-Editor
           und der MenÅ-Editor eine Hilfe-Seite erhalten.
         !end_itemize

 14.12.1999 (!I)GUI-Editor(!i)

         !begin_itemize
         !item Der Fenster-Editor ist nun komplett mit BubbleGEM-
           Informationen ausgerÅstet worden.
         !end_itemize

 10.12.1999 Neue Bibliotheken ACSGEM.LIB und PCGEM.LIB

         !begin_itemize
         !item (!I)ACSpro(!i) ist nun auch um eine eigene GEM-Bibliothek ergÑnzt
           worden. Diese deckt zunÑchst alle VDI- und AES-Funktionen
           bis zum TT-TOS ab. Weitere werden allerdings noch
           "nachgerÅstet". Diese Bibliothek ersetzt die Pure-C-
           Bibliothek PCGEMLIB.LIB vollstÑndig und sollte vîllig
           kompatibel (aber bei wind_get/wind_set sauberer ;-) sein.

           Diese Bibliothek gibt es demnÑchst auch in einer fÅr (!I)ACSpro(!i)
           optimierten und multithreading-festen Form.

         !item Damit wird es auch demnÑchst (also dann, wenn ich einmal
           dazu komme ;-)) zwei Versionen der ACS-Bibliothek geben.
           Die eine baut wie bisher auf der (!link [Pure-C-Bibliothek] [Pure-C-Bibliothek])
           PCGEMLIB.LIB (bzw. wahlweise auf der Ersatzbibliothek
           PCGEM.LIB) auf, die andere ist fÅr die Bibliothek
           ACSGEM.LIB optimiert, enthÑlt also zum Teil wesentlich
           weniger Overhead.
         !end_itemize

 08.09.1999 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Bei mehr als 256 Farben sollte zumindest kein PixelmÅll
           kommen. Bei Checkboxen oder Radiobuttons wird allerdings
           die Schwarz-Weiû-Darstellung verwendet - ein kleiner
           Kompromis, bis ich eine vernÅnftige Lîsung gefunden habe
           ...
         !end_itemize

 30.08.1999 (!I)GUI-Editor(!i)

         !begin_itemize
         !item Der (!I)GUI-Editor(!i) fÅgt nun selbsttÑtig zwei fÅhrende
           Leerzeichen beim Accessory-MenÅeintrag ein, damit dieser im
           MenÅ richtig positioniert wird. Bei der Anzeige werden
           diese fÅhrenden Leerzeichen wieder entfernt. Man muû also
           nicht mehr selbst darauf aufpassen, daû dieser MenÅeintrag
           richtig dargestellt wird.
         !end_itemize

 19.08.1999 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Die Konfigurationsdatei fÅr den (!I)GUI-Editor(!i) wurde erweitert,
           die Seriennummer und der SchlÅssel sind nun auf 10 Zeichen
           erweitert worden. Ebenso ist der (Fenster-)Dialog
           entsprechend erweitert worden.
         !end_itemize

 08.08.1999 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Die (!I)ACSpro(!i)-eigenen Checkbox- und Radio-Button-Userdefs
           waren im Zustand disabled noch nicht an die Checkbox- und
           Radio-Button-Objekte von MagiC 6 angepaût. Die Darstellung
           wurde korrigiert und weiter angeglichen. Ferner wird nun im
           Zustand disabled auch der Beschriftungstext hell
           gezeichnet, wie es bei MagiC 6 der Fall ist. Die Checkbox
           kann nun nicht mehr fÅr ein Tri-State-Objekt verwendet
           werden, dafÅr ist die vîllige MagiC-Anpassung erreicht.

           Bei mehr als 256 Farben sowie bei proportionalen
           SystemzeichensÑtzen gibt es jedoch leider immer noch
           Probleme mit den Chechbox- und Radio-Buttons.
         !end_itemize

 01.08.1999 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Das GEMScript-Protokoll wurde nun passiv in der ersten
           Stufe implementiert. Damit funktioniert nun die
           Fernsteuerung von ACS-Applikationen! Die AufnahmefÑhigkeit
           sowie die aktive Ansteuerung anderer Applikationen ist noch
           nicht implementiert, beides wird jedoch noch
           "nachgerÅstet".

           Bislang werden nur ein paar Standard-GS-Kommandos
           unterstÅtzt, die Liste wird aber noch wachsen.

           Die Standard-Funktion fÅr allgemeine GEMScript-Kommandos
           heiût Aev_GEMScript, fÅr GEMScript-Kommandos mit Bezug auf
           Fenster wird die in Awindow.gemscript eingetragene Funktion
           aufgerufen - Default-Funktion ist hier Awi_gemscript.
         !end_itemize

 24.07.1999 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek]), (!I)GUI-Editor(!i)

         !begin_itemize
         !item FÅr jedes Fenster kann nun der Hilfe-Titel fÅr die ST-
           Guide-Hilfe direkt im (!I)GUI-Editor(!i) eingetragen werden. Dem
           Fenster wird durch die Nachricht AS_ASK_STGUIDE Gelegenheit
           gegeben, das Hilfethema zu verÑndern.

           Falls noch nichts selekiert wurde, so selektiert zum
           Beispiel das Editor-Fenster das Wort unter dem Cursor. Als
           Hilfe-Thema wird der selektierten Bereich verwendet.
         !end_itemize

 22.07.1999 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek]), (!I)GUI-Editor(!i)

         !begin_itemize
         !item Vîllig analog zur BubbleGEM-Hilfe kann nun bereits im (!I)GUI-
           Editor(!i) fÅr jedes (!I)ACSpro(!i)-Userdef, das eine Service-Routine
           besitzt, das (!link [Kontext-MenÅ] [Kontext-MenÅ]) angegeben werden.

         !item Alle Daten, die das Hauptmodul betreffen, sind nun ein
           einem neuen Fensterdialog zusammengefaût. Der bisherige
           Verhalten-Dialog wurde damit wesentlich Åbersichtlicher
           gestaltet.

           In diesen neuen Fensterdialog gelangt man, indem man das
           neue Icon HAUPTMODUL links neben dem bisherigen Icon
           VERHALTEN auswÑhlt.

         !item Der Verhalten-Dialog wurde zusammen mit seinem Icon in
           "Eigenschaft" umbenannt und ist nun fÅr die Eigenschaften
           wie z.B. die Programmiersprache jeder (!I)ACSpro(!i)-Datei
           zustÑndig, wÑhrend die Daten des Hauptmoduls getrennt davon
           im Hauptmodul-Dialog zusammengefaût sind.

         !item Neu ist die Struktur Aconfig. In ihr werden vom (!I)GUI-Editor(!i)
           wichtige Konfigurationsdaten des Hauptmoduls exportiert und
           in der globalen Variable ACSconfig dem Compiler bzw. Linker
           zur VerfÅgung gestellt. Dies sind z.B. die verschiedenen
           Funktionszeiger, die von (!I)ACSpro(!i) in die Struktur Ablk, auf
           die ACSblk verweist, eingetragen werden.

         !item Die wichtigen Funktionszeiger in der Struktur Ablk kînnen
           nun bereits im (!I)GUI-Editor(!i) belegt werden.

         !item Ferner kann nun auch die Funktion ACSinit einen anderen
           Namen tragen. Der Default-Name ist ACSinit, ein
           abweichender Name kann im Hauptmodul-Dialog vorgegeben
           werden.
         !end_itemize

 22.07.1999 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Analog zur BubbleGEM-Hilfe wurde ein (!link [Kontext-MenÅ] [Kontext-MenÅ])
           realisiert. (!I)ACSpro(!i) unterscheidet zwischen langen und kurzen
           Klicks mit der rechten Maustaste. Bei kurzen Klicks
           erscheint die BubbleGEM-Hilfe, bei langen Klicks das
           (!link [Kontext-MenÅ] [Kontext-MenÅ]). Das Kontext-MenÅ kann vom Userdef und vom
           Fenster kontrolliert werden.

         !item Die Strategie zur Behandlung von Klicks mit der rechten
           Maustaste ist nun wie folgt:

             !begin_enumerate
             !item Falls nach einer Zeit von im Moment festen 250 msec.
                die rechte Maustaste noch gedrÅckt ist, handelt es
                sich um einen langen Klick, andernfalls um einen
                kurzen Klick.

             !item Bei einem kurzen Klick wird die BubbleGEM-Hilfe
                aufgerufen, andernfalls das (!link [Kontext-MenÅ] [Kontext-MenÅ]).

             !item Falls keine BubbleGEM-Hilfe verfÅgbar war, wird das
                (!link [Kontext-MenÅ] [Kontext-MenÅ]) verwendet.

             !item Falls kein (!link [Kontext-MenÅ] [Kontext-MenÅ]) verfÅgbar war, wird nach der
                Wartezeit zur Ermittlung eines langen Klicks (s.o.)
                die BubbleGEM-Hilfe angezeigt.
             !end_enumerate

         !item Es wurden zwei neue Funktionen implementiert, Aob_state und
           Aob_flags. Sie dienen zur einfachen Manipulation der
           Komponenten ob_state bzw. ob_flags der Struktur OBJECT in
           einem Objektbaum (Workobjekt oder Toolbar) eines Fensters.
         !end_itemize

 20.07.1999 (!I)GUI-Editor(!i)

         !begin_itemize
         !item Die Texte fÅr BubbleGEM kînnen nun fÅr jedes ACS-Userdef,
           das eine Service-Routine besitzt, komfortabel bereits im
           (!I)GUI-Editor(!i) angegeben werden. Als besonderes Feature kînnen
           die BubbleGEM-Strings am Beispiel-Objekt in der linken
           oberen Ecke direkt beim Editieren ausprobiert werden, es
           wird sofort der eingegebene Text verwendet.
         !end_itemize

 27.06.1999 Bibliotheken (!link [ACS.LIB] [Pure-C-Bibliothek]) & (!link [ACSPLUS.LIB] [C-Erweiterungs-Bibliothek]), (!I)GUI-Editor(!i)

         !begin_itemize
         !item Die Struktur Awindow wurde erweitert:

             !begin_enumerate
             !item Es sind zwei Funktionszeiger hinzugekommen, die fÅr
                Iconify benîtigt werden. Die Default-Routinen sind
                Awi_iconify und Awi_uniconify.

             !item FÅr die ST-Guide-UnterstÅtzung sind ferner zwei neue
                Strings vorhanden. Hier kann das Hilfe-Thema fÅr
                dieses Fenster sowie ggf. der Dateiname des
                Hypertextes angegeben werden.

             !item Zur UnterstÅtzung des GEMScript-Protokolles ist ein
                Funktionszeiger definiert, der auf die GEMScript-
                Routine des Fensters zeigt. Default-Funktion ist hier
                Awi_gemscript. Im Moment macht diese Routine noch
                nichts, aber das Ñndert sich bald... ;-)
             !end_enumerate

         !item Die Struktur AUSERBLK der Userdefs wurde um folgende
           Komponenten erweitert:

             !begin_enumerate
             !item Ein neuer String enthÑlt den ggf. zu verwendenden Text
                fÅr eine BubbleGEM-Hilfe.

             !item Ein weiterer String steht fÅr ein kontextsensitives
                Popup zur VerfÅgung.
             !end_enumerate

         !item Ebenso hat die Struktur AUSER_DEF ebenfalls zwei neue
           Zeiger bekommen, einen fÅr die BubbleGEM-Hilfe, einen fÅr
           das Kontext-Popup. Damit mÅssen alle vorhandenen Editoren
           an diese énderung angepaût werden - sorry!
         !end_itemize

 27.06.1999 (!I)GUI-Editor(!i)

         !begin_itemize
         !item Das Dateiformat ist nun umgestellt worden. Der (!I)GUI-Editor(!i)
           liest weiterhin alle alten Formate und konvertiert sie in
           das neue Format, allerdings werden diese nach der
           Umwandlung als verÑndert gekennzeichnet. Daraufhin wird
           beim Beenden des (!I)GUI-Editor(!i) gefragt, ob die (!I)ACSpro(!i)-Datei
           gesichert werden soll.

           Alle (!I)ACSpro(!i)-Dateien im alten Format mÅssen aufgrund der
           neuen Komponenten in den Strukturen Awindow und AUSERBLK
           umgestellt werden. Andernfalls drohen AbstÅrze (zwei oder
           drei Bîmbchen) unter anderem bei der ST-Guide-Hilfe,
           Iconify und GEMScript.
         !end_itemize

 17.06.1999 Bibliothek (!link [ACSPLUS.LIB] [C-Erweiterungs-Bibliothek]) & (!I)GUI-Editor(!i)

         !begin_itemize
         !item Das Editor-Fenster und der (!I)GUI-Editor(!i) senden nun nach dem
           Speichern per Aev_OlgaUpdate eine entsprechende Message an
           den OLGA-Manager.

           Damit kann z.B. der (!I)GUI-Editor(!i) mit Hilfe des integrierten
           Editor-Fensters als OLGA-fÑhiger Texteditor eingesetzt
           werden - das habe ich natÅrlich gleich ausprobieren
           mÅssen... ;-)
         !end_itemize

 17.06.1999 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Das OLGA-Protokoll wird nun als (!link [OLGA-Server] [OLGA-Server]) unterstÅtzt.
           Das Nachstarten des OLGA-Managers sowie das An- und
           Abmelden am OLGA-Manager funktionieren vollautomatisch. Die
           Funktionen Aev_OlgaUpdate, Aev_OlgaRename und
           Aev_OlgaBreaklink stehen bereits zur VerfÅgung und arbeiten
           korrekt.

           Eine komfortablere Variante ist in GrundzÅgen bereits
           implementiert und wird gerade ausgebaut. Wesentliche Teile
           der (!link [OLGA-Client] [OLGA-Client])-Seite sind ebenfalls fertig, allerdings
           wird hier noch gefeilt... ;-)

           Die volle OLGA-UnterstÅtzung auch als Client wird in der
           Version 3.0.0 noch nicht zur VerfÅgung stehen. Sie ist
           jedoch in Arbeit und steht auch weiterhin auf der ToDo-
           Liste.
         !end_itemize

 30.05.1999 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Das komplette PureC-Hilfe-Protokoll steht nun zur
           VerfÅgung.

           Allerdings habe ich bei den diversen Tests festgestellt,
           daû PC_HELP.ACC die RÅckmeldung stets an die Applikation 0
           meldet. In Multitasking-Systemen geht dies dann "daneben".

           Dies ist ein Bug in PC_HELP.ACC und kann von (!I)ACSpro(!i) nicht
           umgangen werden. Wenn der ST-Guide Åber dieses (!link [Protokoll] [Protokoll])
           angesteuert wird, funktioniert alles reibungslos!

         !item Der ST-Guide von Holger Weets kann nicht nur Åber das
           PureC-Hilfe-Protokoll angesteuert werden, sondern versteht
           auch das AV-(!link [Protokoll] [Protokoll]). Die Funktion Aev_STGuideHelp stellt
           das komfortable Inferface fÅr die Ansteuerung des ST-Guide
           dar.

           Der ST-Guide scheint nach der Nachricht VA_START die
           Antwort VA_STARTED an den AV-Server zu senden und nicht an
           die absendende Anwendung. Damit ist keine Mîglichkeit
           gegeben, komfortable RÅckmeldungen zu bekommen. Zum GlÅck
           klappt mit dem PureC-Hilfe-Protokoll alles bestens... ;-)

           Dies ist ein Bug im ST-Guide und kann von (!I)ACSpro(!i) nicht
           umgangen werden. FÅr beide Varianten, die Online-Hilfe
           anzusprechen, habe ich entsprechende Workarounds
           implementiert. (!I)ACSpro(!i) ist also auf dieses "besondere
           Verhalten" beider Hilfe-Accessories vorbereitet und sollte
           richtig darauf reagieren... ;-)

         !item Die Strategie, um den AV-Server zu finden, wurde etwas
           geÑndert. Zuerst wird die Environment-Variable AVSERVER
           ausgewertet. Falls diese nicht existiert oder der dort
           angegebene AV-Server nicht gefunden werden kann, werden (in
           dieser Reihenfolge) jinnee, Gemini, AVServer und MagxDesk
           als AV-Server versucht.
         !end_itemize

 18.05.1999 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Neue Funktion Awi_ontop liefert das oberste Fenster. Dies
           wird auch behalten, wenn (!I)ACSpro(!i)-Anwendungen in den
           Hintergrund treten.

         !item Wenn (!I)ACSpro(!i) in Multitasking-Systemen wieder nach vorne kam,
           wurde direkt ein Maus-Klick ausgelîst. Dies ist nun
           abgestellt!
         !end_itemize

 08.05.1999 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Iconify in allen Varianten wird nun direkt vom (!I)ACSpro(!i)-
           System erledigt. Die Realisierung Åber die Service-Routine
           ist somit hinfÑllig. DemnÑchst werden bei der sowieso
           anstehenden énderung der Awindow-Struktur entsprechende
           Funktionszeiger fÅr die Nachrichten WM_ICONIFY,
           WM_ALLICONIFY und WM_UNICONIFY aufgenommen werden.
         !end_itemize

 04.05.1999 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Die XAcc-Empfangsroutinen wurden komplett Åberarbeitet und
           diverse kleinere Bugs ausgebaut.
         !end_itemize

 04.05.1999 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Durch die Nachricht AS_ACC_ACK wird mitgeteilt, daû ein von
           (!I)ACSpro(!i) ausgelîster XAcc-Transfer abgeschlossen wurde.
           Zugleich wird der Status des Transfers mitgeteilt
           (Erfolg/Miûerfolg). Das Fenster kann dann geeignet darauf
           reagieren (etwa nach einiger Zeit einen neuen Transfer
           versuchen o.Ñ.).

           Die auslîsenden Routinen werden demnÑchst (Allerdings noch
           nicht in Version 3.0.0) in (!I)ACSpro(!i) zur VerfÅgung stehen, so
           daû dann auch der "aktive Datentransfer" per XAcc-(!link [Protokoll] [Protokoll])
           direkt unterstÅtzt wird.
         !end_itemize

 02.05.1999 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Beim Lîschen einer linearen Liste gab es einen Crash. Die
           Routine stÅrzte ab, wenn (!I)ACSpro(!i) beendet wird und dabei
           Alu_delete aufgerufen wurde. Der Bug ist behoben.

         !item Optimierung: Umstellen der ACS-internen Message-
           Verarbeitung auf BinÑrsuche. Damit ist die
           ErweiterungsfÑhigkeit fÅr GEMScript, OLGA, BubbleGEM o.Ñ.
           (siehe ToDo-Liste) gegeben, da sich bei diesen vielen
           Nachrichten, auf die es zu reagieren gilt, die Vergleiche
           bis zur Identifikation der Nachricht drastisch reduzieren.
           Bei 61 Nachrichten (Stand 17.04.2001) wÑren es nach dem bis
           einschl. Version 2.3.x verwendeten Verfahren im Mittel 30
           Vergleiche, jetzt sind es (!I)maximal(!i) nur 7 Vergleiche, bis die
           Nachricht einwandfrei identifiziert ist.

           Die Funktionen fÅr die Drag&Drop-Protokolle werden
           allerdings noch komplett Åberarbeitet, damit (!I)ACSpro(!i) nicht
           nur den eher "passiven Empfang", sondern auch das "aktive
           Senden" in gewohntem Komfort beherrscht.
         !end_itemize

 14.04.1999 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
         !item Die Routine ACSinit0 wird nun (!I)vor(!i) der Initialisierung der
           XAcc-Daten und dem Anmelden bei den diversen Protokollen
           (XAcc, VA, etc.) aufgerufen.
         !end_itemize

!end_node


!begin_node énderungen Version V2.32 auf V2.3.3

Hier sind die énderungen und Erweiterungen von V2.32 zur V2.3.3 in
umgekehrter chronologischer Reihenfolge aufgelistet.

(!B)Hinweis:(!b) Die Version 2.3.3 enthÑlt nur wichtige Korrekturen von
Fehlern der Version 2.32.

(!B)Hinweis:(!b) énderungen an den C-Bibliotheken betreffen stets auch die
(!link [Pascal-Bibliothek] [Pascal-Bibliothek]). Von Bugfixes und Erweiterungen in den Bibliotheken
hat stets auch der (!I)GUI-Editor(!i) profitiert, da dieser ja mittels (!I)ACSpro(!i)
entwickelt wurde.

Stand der Liste ist der 13.06.2000.

 13.06.2000 PCTOS.LIB

         !begin_itemize
           !item Nachgezogene Korrektur fÅr V2.32 (analog zu V3.0.0):

             In der Bibliothek PCTOS.LIB hatte sich fÅr Mshrink ein
             Fehler eingeschlichen, der Parameter zero war nicht
             vorhanden. Auswirkungen hatte dies natÅrlich bei allen
             Anwendungen, die diese Bibliothek verwenden, u.a. im
             (!I)GUI-Editor(!i) beim Laden von Modulen.
         !end_itemize

 09.06.2000 (!I)GUI-Editor(!i)

         !begin_itemize
           !item Nachgezogene Korrektur fÅr V2.32 (analog zu V3.0.0):

             Beim Lîschen einer linearen Liste gab es einen Crash. Die
             Routine stÅrzte ab, wenn (!I)ACSpro(!i) beendet wird und dabei
             Alu_delete aufgerufen wurde. Der Bug ist behoben.

           !item Nachgezogene Korrektur fÅr V2.32 (analog zu V3.0.0):

             Kleineren Bug behoben: Die Funktion Awi_ontop() lieferte
             nach der Nachricht WM_ONTOP fÑlschlicherweise das Root-
             Fenster zurÅck... ;-/
         !end_itemize

 02.06.2000 (!I)GUI-Editor(!i)

         !begin_itemize
           !item Nachgezogene Korrektur fÅr V2.32 (analog zu V3.0.0):

             Der (!I)GUI-Editor(!i) hat fÅr Pure-Pascal die Texte des
             Hauptmoduls nicht exportiert (Der Bug war eigentlich
             immer schon so drin, das hat noch nie richtig
             funktioniert... ;-/). Damit stÅrzten Programme ab, wenn
             per Ash_error oder Awi_alert Meldungen ausgegeben werden
             sollte, ohne daû alert_name gesetzt war.
         !end_itemize

 18.05.2000 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek]), (!I)GUI-Editor(!i)

         !begin_itemize
           !item Die Versionsnummern wurden nun umbenannt. Aus V2.33 ist
             V2.3.3 geworden.
         !end_itemize

 04.04.2000 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
           !item Nachgezogene Korrektur fÅr V2.32 (analog zu V3.0.0):

             Die Routine Awi_doform warf Bîmbchen, wenn das Erzeugen
             des Fensters nicht funktioniert hat und auf A_dialog
             ausgewichen werden muûte.
         !end_itemize

 29.03.2000 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
           !item Nachgezogene Korrektur fÅr V2.32 (analog zu V3.0.0):

             Die Funktion Awi_obchange funktionierte bei einem Wechsel
             des Objekt-Status von DISABLED auf ENABLED nicht richtig.
             Das Objekt war zwar enabled, wurde aber z.T disabled
             gezeichnet. Meine Untersuchungen haben ein etwas
             merkwÅrdiges Verhalten von objc_change ergeben. Das
             Problem wurde durch einen Workaround umschifft.

           !item Nachgezogene Korrektur fÅr V2.32 (analog zu V3.0.0):

             In Ast_ltrim - und damit auch in Ast_alltrim - war noch
             ein Bug, der das Ende des korrigierten Strings falsch
             setzte.
         !end_itemize

 13.02.2000 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
           !item Nachgezogene Korrektur fÅr V2.32 (analog zu V3.0.0):

             Wenn (!I)ACSpro(!i) in Multitasking-Systemen wieder nach vorne
             kam, wurde direkt ein Maus-Klick ausgelîst. Dies ist nun
             abgestellt!

           !item Nachgezogene Korrektur fÅr V2.32 (analog zu V3.0.0):

             Im Dialog "Allgemein" des Module-Fensters funktionierte
             die Einstellung "Kopiere Bildteil" nicht richtig. Wenn
             die Option umgestellt wurde, wurde stets das Gegenteil
             durchgefÅhrt. Wenn die Option in der Default-Einstellung
             belassen wurde, klappte alles wie es sollte. Dies ist nun
             korrigiert! Die Einstellungen in diesem Dialog sollten
             daher nochmals kontrolliert werden.
         !end_itemize

 25.01.2000 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
           !item Nachgezogene Korrektur fÅr V2.32 (analog zu V3.0.0):

             Die Funktion Af_readdir - und damit auch Af_first und
             Af_next - funktionierte nicht korrekt, wenn Dxreaddir und
             Dreaddirr (MagiC bzw. MiNT) nicht vorhanden sind und
             intern Fsfirst/Fsnext verwendet wurde. Die Dateinamen
             wurden mit "*.*" aufgeblÑht:

                        Laufwerk:\ORDNER1\...\*.*\DATEI.XYZ

             Das Problem ist nun behoben!
         !end_itemize

!end_node


!begin_node énderungen Version V2.31 auf V2.32

Hier sind die énderungen und Erweiterungen von V2.31 zur V2.32 in
umgekehrter chronologischer Reihenfolge aufgelistet.

(!link [Zwei Probleme existieren noch] [ToDo-Liste]): (!I)ACSpro(!i) hat noch Schwierigkeiten mit
mehr als 256 Farben und mit einem proportionalen Systemzeichensatz.
Beides sind etwas aufwendigere Korrekturen, die ich aus diesem Grunde
nach hinten verschoben habe.

énderungen an den C-Bibliotheken betreffen stets auch die Pascal-
Bibliothek. Von Bugfixes und Erweiterungen in den Bibliotheken hat
stets auch der (!I)GUI-Editor(!i) profitiert, da dieser ja mittels (!I)ACSpro(!i)
entwickelt wurde.

Stand der Liste ist der 09.04.1999.

 09.04.1999 (!I)ACSpro(!i)-(!I)GUI-Editor(!i)

         !begin_itemize
           !item Im Fenster-Editor konnten die Grenzen zwischen den
             verschiedenen Fenterelementen verschwinden. Bei kleinerem
             Rahmen stand unter MultiTOS kein Text mehr in den
             Elementen. Die gesamte Darstellung der Fensterelemente
             ist nun vîllig Åberarbeitet worden.
         !end_itemize

 09.04.1999 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek]) & ACS_LOAD.O

         !begin_itemize
           !item Die Modul-Schnittstelle enthÑlt nun auch alle neuen
             Funktionen. Sie ist abwÑrtskompatibel erweitert und
             optimiert worden.
         !end_itemize

 09.04.1999 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
           !item Interne Optimierungen in der Event-Schleife und beim
             Draggen der Auswahlliste.
         !end_itemize

 01.04.1999 Bibliothek (!link [ACSPLUS.LIB] [C-Erweiterungs-Bibliothek])

         !begin_itemize
           !item Der Desktop PUR_DESK hat unter gewissen UmstÑnden die
             internen Flags "verpfuscht". Damit ist das Flag Smart-
             Redraw wieder ausgeschaltet worden. Ein bedauerliches
             Versehen, welches nun korrigiert ist.
         !end_itemize

 29.03.1999 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek]) & (!I)GUI-Editor(!i)

         !begin_itemize
           !item Das Backdropping von angemeldeten Fenstern wird nun
             automatisch erledigt, die entsprechenden Fenster mÅssen
             im (!I)GUI-Editor(!i) mit Backdrop-Element angemeldet sein. Bei
             Systemen ohne Backdrop-Funktion werden die Fenster
             automatisch (!I)ohne(!i) dieses Fensterelement angelegt.

             Bei modalen Fenster-Dialogen wird bei angemeldetem
             Backdrop das Fenster natÅrlich (!I)nicht(!i) in den Hintergrund
             gelegt (Ich suche noch nach einer Mîglichkeit, bei
             modalen Fenstern nicht das Fenster, sondern die Anwendung
             in den Hintergrund zu bringen.).

             Bei Fenster, die den Backdrop-Button nicht angemeldet
             haben, wird der Button unter MagiC hinzugefÅgt und von
             MagiC bedient. In diesem Fall kann ein modaler Fenster-
             Dialog in den Hintergrund geklickt werden. Bevor
             allerdings ein anderes Fenster getoppt werden kann, wird
             automatisch wieder der modale Fenster-Dialog nach vorne
             gebracht - ein unschînes Flackern ist die Folge.

             In der Version 3.0 wird es hier eine Erweiterung der
             Awindow-Struktur geben, in der auch das Backdropping
             berÅcksichtigt ist!

             (!I)Es ist dringendst anzuraten, die Backdrop-Komponente fÅr
             alle Fenster anzumelden, da dann (!i)(!I)ACSpro(!i)(!I) bzw. die
             Anwendung die Kontrolle Åber das Verhalten beim Backdrop
             hat.(!i)
         !end_itemize

 27.03.1999 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
           !item Die (!I)ACSpro(!i)-eigenen Checkbox- und Radio-Button-Userdefs
             wurden vom Aussehen her an die Checkbox- und Radio-
             Button-Objekte von MagiC 6 angepaût. Das Aussehen der
             Userdefs entspricht nun sowohl im 3D-Modus als auch im
             2D-Modus genau den MagiC-Objekten. Bei mehr als 256
             Farben gibt es jedoch leider immer noch Probleme mit den
             Chechbox- und Radio-Buttons.
         !end_itemize

 26.03.1999 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
           !item Ein kleiner Bug war noch in Af_readdir: die Root-
             Verzeichnisse wurden nicht korrekt behandelt (ein '\'
             zuviel hinten abgeschnitten). Das ist nun behoben.
         !end_itemize

 24.03.1999 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
           !item Der Vergleich der Dateiextension beim Laden der
             (Autostart-)Module wurde intern von strcmp auf die
             Funktion Ast_icmp umgestellt, damit spielt die Groû-/
             Kleinschreibung der Extension beim Laden der Module keine
             Rolle mehr! Das Laden von Modulen sollte nun auch auf
             casesensitiven Systemen (MiNT mit MinixFS) funktionieren.
         !end_itemize

 19.03.1999 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
           !item Die Funktion Af_readdir zum Einlesen eines kompletten
             Verzeichnisinhaltes wurde komplett neu programmiert und
             optimiert. Sie benutzt nun nicht mehr die beiden
             archaischen Funktionen Af_first und Af_next und sollte
             gegenÅber diesen beiden auch bevorzugt werden!

           !item Die Funktionen Af_first und Af_next wurden komplett neu
             programmiert und nutzen jetzt die Funktion Af_readdir.

             (!B)Hinweis:(!b) (!I)Af_first und Af_next sind nur zu
             KompatibilitÑtszwecken noch vorhanden und werden
             wahrscheinlich (wenn sich kein zu groûer Widerspruch regt
             ;-) in einer der nÑchsten Versionen wegfallen.(!i)
         !end_itemize

 14.02.1999 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
           !item Die UnterstÅtzung von Konfigurationsdateien im ASCII-
             Format wurde implementiert - der Datentyp UConfig mit
             einigen (!link [Funktionen] [Die Konfigurations-Strings]) zur Bearbeitung. Diese ASCII-Dateien
             besitzen einen Aufbau analog zu den bekannten INI-Dateien
             in der Windows-Welt. Ab der Version 2.4 von (!I)ACSpro(!i) werden
             diese von mir Konfigurations-Strings (oder Config-
             Strings, Cfg-Strings) genannten
             Konfigurationsmîglichkeiten vollkommen transparent in
             (!I)ACSpro(!i) integriert sein und vom (!I)GUI-Editor(!i) mit den
             zugehîrigen Initialisierungsdaten versehen werden kînnen.
             Noch ist dies nicht der Fall, dafÅr mÅûte ich das Format
             der *.ACS-Dateien inkompatibel Ñndern. Das wird erst in
             V2.4 (incl. automatischer Konversion natÅrlich!)
             geschehen.
         !end_itemize

 01.02.1999 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
           !item Es wurden einige nÅtzliche Funktionen fÅr den Umgang mit
             Strings implementiert. Diese sind:

              !begin_xlist [Ach_tolower]
              !item [Ach_tolower]  Buchstaben in Kleinbuchstaben wandeln -
                           Umlaute werden korrekt berÅcksichtigt

              !item [Ach_toupper]  Buchstaben in Groûbuchstaben wandeln -
                           Umlaute werden korrekt berÅcksichtigt

              !item [Ach_isWhite]  Zeichen auf "Blank" (Leerzeichen,
                           Tabulator, Carriage-Return, Line-Feed)
                           ÅberprÅfen

              !item [Ast_cmp]      Zwei Strings vergleichen, Groû-/
                           Kleinschreibung (!I)wird(!i) berÅcksichtigt, die
                           Umlaute werden korrekt behandelt

              !item [Ast_icmp]     Zwei Strings vergleichen, Groû-/
                           Kleinschreibung wird (!I)nicht(!i) berÅcksichtigt,
                           die Umlaute werden korrekt behandelt

              !item [Ast_isEmpty]  PrÅfen, ob ein String nur Blanks (bestimmt
                           durch Ach_isWhite) enthÑlt, also eigentlich
                           "leer" ist

              !item [Ast_add]      Strings hintereinanderhÑngen und auf
                           bestimmte LÑnge mit Leerzeichen auffÅllen

              !item [Ast_adl]      String linksbÅndig mit Leerzeichen
                           auffÅllen

              !item [Ast_adr]      String rechtsbÅndig mit Leerzeichen
                           auffÅllen

              !item [Ast_adc]      String zentriert mit Leerzeichen auffÅllen

              !item [Ast_filter]   "Nicht erlaubte" Zeichen aus String
                           herausnehmen

              !item [Ast_count]    HÑufigkeit von Zeichen im String zÑhlen

              !item [Ast_fcmp]     Dateinamen vergleichen, der erste darf die
                           Åblichen Wildcards '*' und '?'
                           enthalten (Der zweite Dateiname darf keine
                           Wildcards enthalten, evtl. werde ich diese
                           aber doch noch einbauen, 'mal sehen... ;-))
              !end_xlist
         !end_itemize

 31.01.1999 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
           !item Die Service-Funktionen der unsortierten, linearen Liste
             ULinList sind nun nur noch Åber die Zeiger der Struktur
             ULinList verfÅgbar. Sie sind dem direkten Zugriff durch
             die Realisierung als static-Funktionen verborgen worden.
             Dies schÅtzt vor fÑlschlichem Gebrauch der Funktionen und
             schÅtzt einmal entwickelten Code vor Arbeiten an der
             Liste und den Service-Funktionen.
         !end_itemize

 28.01.1999 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
           !item Die Funktion Ast_ltrim wurde implementiert. Sie entfernt
             fÅhrende Leerzeichen, d.h. entfernt Leerzeichen links vom
             "eigentlichen String".

           !item Die Funktion Ast_rtrim entfernt analog abschlieûende
             Leerzeichen, d.h. Leerzeichen, d.h. entfernt Leerzeichen
             rechts vom "eigentlichen String".

           !item Die Funktion Ast_alltrim ist die Kombination der beiden
             Funktionen Ast_ltrim und Ast_rtrim und entfernt fÅhrende
             und abschlieûende Leerzeichen eines Strings.
         !end_itemize

 21.01.1999 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
           !item Das "Shaden" der Fenster unter WINX und MagiC
             funktioniert nun. Hier gab es Fehler mit den Maus-Formen.
             War z.B. im obersten Fenster ein editierbares Objekt, so
             wurde die Maus-Form auch dann noch auf den "Cursor"
             gesetzt, wenn das Fenster unter MagiC per Doppelklick
             zusammengeklappt wurde.

             (!I)ACSpro(!i) kann nun mit dem Shaded-Zustand umgehen. Die
             Meldungen WM_SHADED und WM_UNSHADED werden ausgewertet.
             Der neue Fenster-Zustand (!link [AWS_SHADED] [AWS_SHADED]) gibt Auskunft
             darÅber, ob das Fenster zusammengeklappt wurde. Ferner
             werden dem Fenster die (neuen) Nachrichten AS_SHADED und
             AS_UNSHADED gesandt, um es davon zu informieren.

           !item Das Flag AB_SMARTREDRAW wird nun beim Initialisieren
             gesetzt. In ACSinit0 oder durch das Modul Ab_Cfg kann es
             wieder abgeschaltet werden. Unter MagiC ((!link [MagiCMac] [Mag!X]),
             (!link [MagiCPC] [Mag!X])) und N.AES werden so Redraw-Fehler vermieden, da
             hier meist das sog. Smart-Redraw aktiviert ist. Unter
             MagiC kînnte zwar abgefragt werden, ob es eingeschaltet
             ist, aber diese Lîsung ist auf MagiC beschrÑnkt.

             In Zukunft wird u.a. das Konfigurieren der Flags
             wesentlich einfacher gestaltet, ich bin bereits am
             tÅfteln und programmieren... ;-)
         !end_itemize

 21.01.1999 (!I)ACSpro(!i)-(!I)GUI-Editor(!i)

         !begin_itemize
           !item Optimierung: Der (!I)GUI-Editor(!i) hatte beim Start
             Einstellungen gesetzt, die beim Start bereits von den
             Routinen der (!link [ACS.LIB] [Pure-C-Bibliothek]) durchgefÅhrt wurden.
         !end_itemize

 19.01.1999 Bibliotheken (!link [ACS.LIB] [Pure-C-Bibliothek]) und (!link [ACSPLUS.LIB] [C-Erweiterungs-Bibliothek]), (!I)ACSpro(!i)-(!I)GUI-Editor(!i)

         !begin_itemize
           !item Die Optik der Dialoge und Fenster wurde etwas
             Åberarbeitet und vereinheitlicht. Dies betrifft besonders
             die als "Untergrund" eingesetzten 3D-Buttons, die nun ein
             durchgÑngigeres Aussehen haben.
         !end_itemize

 15.01.1999 Der (!I)ACSpro(!i)-(!I)GUI-Editor(!i)

         !begin_itemize
           !item Der (!I)GUI-Editor(!i) besitzt nun in der freigeschalteten
             Version einen eigenen Info-Dialog. Dieser wird per
             Awi_doform dargestellt. Durch Weiterverzweigen in
             Unterdialoge kann die Versionsnummer sowie Kurz-
             Informationen zum Update bzw. Upgrade - u.a. die Stellen
             zum Download - angezeigt werden.
         !end_itemize

 15.01.1999 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
           !item Es wurde die neue Funktion Awi_doform implementiert. Die
             Funktion verwendet Awi_dialog. Falls kein Fenster
             erstellt werden kann, wird automatisch A_dialog
             verwendet. Das verwendete Fenster wird ohne Fuller und
             Sizer dargestellt. Die Scrollbalken fÅr vertikales und
             horizontales Scrollen werden nur dann verwendet, wenn der
             Dialog nicht komplett angezeigt werden kann.
         !end_itemize

 02.01.1999 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
           !item (!I)ACSpro(!i) verwendet nun die NKCC-Bibliothek (von Harald
             Siegmund) in der Version 2.94 vom 21.09.1998.
         !end_itemize

 22.10.1998 Bibliothek (!link [ACSPLUS.LIB] [C-Erweiterungs-Bibliothek])

         !begin_itemize
           !item Der pure Desktop sucht und schreibt seine
             Konfigurationsdatei nun mit Kleinbuchstaben, d.h. sie
             wird als "pdesk.cfg" gesucht. Damit sollte der pure
             Desktop seine Konfigurationsdatei auch unter
             casesensitiven Dateisystemen (unter MiNT auf MinixFS)
             finden.
         !end_itemize

 11.10.1998 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
           !item Neue String-Funktion Ast_toupper und Ast_tolower zur
             Konversion von Strings implementiert.
         !end_itemize

!end_node


!begin_node énderungen Version V2.3 auf V2.31

Hier sind die énderungen und Erweiterungen von V2.3 zur V2.31 in
umgekehrter chronologischer Reihenfolge aufgelistet. Momentan sieht es
noch nach recht wenig aus, doch muû hier auch ein gutes StÅck
Einarbeitungszeit in die Sourcen von (!I)ACSpro(!i) einkalkuliert werden. Da
der Atari-Sektor recht klein geworden ist, muû ich anderweitig Geld
verdienen, so daû (!I)ACSpro(!i) - leider - nur mein Hobby ist.

Hier sind also die énderungen! énderungen an den C-Bibliotheken
betreffen stets auch die (!link [Pascal-Bibliothek] [Pascal-Bibliothek]). Von Bugfixes und
Erweiterungen in den Bibliotheken hat stets auch der (!I)GUI-Editor(!i)
profitiert, da dieser ja mittels (!I)ACSpro(!i) entwickelt wurde.

Stand der Liste ist der 30.09.1998.

 30.09.1998 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
           !item Die neue Funktion Awi_diaabort kann als (!link [Click-Routine] [Click-Routine]) in
             einem der modalen Fensterdialogen eingesetzt werden. Sie
             setzt das Flag ACSblk->dia_abort auf TRUE und tut sonst
             nichts weiter. Auch kurze Meldungen, die in einem Fenster
             mit einem OK-Button dargestellt werden, kînnen diese
             Routine einsetzen.

             Sie wird beispielsweise in der Info-Box eingesetzt, die
             jedes mittels (!I)ACSpro(!i) erstellte Programm defaultmÑûig
             anzeigt - sofern die in ACSblk->ACSaboutme eingetragene
             Routine nicht Åberschrieben wird. Der hier verwendete
             3D-Button im Hintergrund hat das Flag TOUCHEXIT gesetzt
             und als (!link [Click-Routine] [Click-Routine]) Awi_diaabort eingetragen bekommen.
         !end_itemize

 31.08.1998 (!I)GUI-Editor(!i)

         !begin_itemize
           !item Die Text-Eingabe im (!I)GUI-Editor(!i) zur Eingabe von
             Texten/freien Strings nimmt nun auch Sonderzeichen
             (speziell <32) an. Damit sind z.B. das Ordner-Symbol
             sowie die Pfeil-Symbole mîglich. (fÅr énderungen an
             MenÅ-EintrÑgen sehr nÅtzlich ;-)
         !end_itemize

 28.08.1998 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
           !item Die Routine Awi_topped beachtet fÅr modale Fenster-
             Dialoge nun die Flags AB_CENTERDIALOG und AB_ACUSTIC.

             Damit verhalten sich modale Fenster-Dialoge (Awi_dialog)
             so wie die modalen Dialoge (A_dialog): Ein Klick neben
             den Dialog bringt bei gelîschtem AB_CENTERDIALOG den
             Dialog an die Mausposititon, bei gesetztem
             AB_CENTERDIALOG wird ein "Pling" Bconin(2,7) ausgegeben,
             falls auch AB_ACUSTIC gesetzt ist,
         !end_itemize

 27.08.1998 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
           !item Die unsortierte, lineare und doppelt verkettete Liste
             ULinList mit einigen netten Funktionen ist dazugekommen.
             Das Objekt hat keine visuelle Komponente (wozu auch) und
             stellt die FunktionalitÑt einer dynamischen, linearen,
             unsortierten Liste zur VerfÅgung. Die Liste verwaltet
             Zeiger auf Daten (in Pure- C: void *, in Pure-Pascal:
             Pointer). ZusÑtzlich sind einige nÅtzliche Funktionen
             enthalten, die man im Zusammenhang mit linearen Listen
             immer wieder einmal benîtigt.
         !end_itemize

 21.08.1998 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
           !item Die Funktion Awi_keys wurde in Awi_keysend umbenannt.

           !item Es gibt eine neue Routine Awi_keys, die den RÅckgabewert
             -2 nicht mehr liefert. Da intern Awi_keysend aufgerufen
             wird, stellt Awi_keys die gewohnte FunktionalitÑt zur
             VerfÅgung. Allerdings werden TastendrÅcke nicht mehr an
             den AV-Server weitergemeldet. Wer dies unbedingt mîchte,
             sollte auf Awi_keysend zurÅckgreifen.

           !item Awi_nokey liefert nun stets den RÅckgabewert -1 (zuvor
             war es -2) und leitet damit die TastendrÅcke (!I)nicht(!i) mehr
             an den AV-Server weiter.

           !item Die neue Routine Awi_sendkey liefert nun stets den
             RÅckgabewert -2 und veranlaût damit, daû TastendrÅcke an
             den AV-Server weitergeleitet werden.

             Es gibt damit vier Key-Routinen fÅr Fenster: Awi_keys,
             Awi_keysend, Awi_sendkey und Awi_nokey.

             Kurz zur Namensgebung der vier Routinen:

              !begin_xlist [Awi_keysend]
              !item [Awi_keys]     Key auswerten

              !item [Awi_keysend]  Zuerst Key auswerten, dann ggf.
                           weitersenden

              !item [Awi_sendkey]  Sende den Key sofort weiter

              !item [Awi_nokey]    Key "schlucken" und ignorieren
              !end_xlist

           !item Redraw-Probleme in der Toolbar beim Smart-Redraw unter
             MagiC auf "drastische" Weise behoben (ggf. Neuzeichnen
             des Fensters). Zuvor blieben evtl. Streifen des Toolbar-
             Randes stehen.
         !end_itemize

 02.08.1998 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
           !item Die Funktion Dxreaddir aus (!link [ACS.LIB] [Pure-C-Bibliothek]) herausgenommen. Der
             (!I)ACSpro(!i)-(!I)GUI-Editor(!i) verwendet nun die Bibliothek
             ACSTOS.LIB. Dort sind alle BIOS-, XBIOS- und GEMDOS-
             Aufrufe abgelegt, die ich im Profibuch und im Compendium
             gefunden habe. Die MagiC-spezifischen Funktionen fehlen
             noch, werden aber noch nachgerÅstet!

           !item Af_first und Af_next sowie Af_readdir lieferten etwas
             dÅrftige Informationen Åber die Dateien, es war gerade
             einmal der Name. Da innerhalb dieser Funktionen die Daten
             sowieso zur VerfÅgung stehen, werden nun alle
             interessanten Daten der gefundenen Dateien geliefert.
             Hierzu wurde die Struktur A_FileList um eine Komponente
             vom Datentyp XATTR erweitert, in der die Dateiattribute,
             Grîûe, etc. zu finden sind. Die Werte werden per
             Dxreaddir bzw. Dreaddir/Fxattr bestimmt. Falls diese
             Funktionen nicht zur VerfÅgung stehen (z.B. unter TOS),
             werden die Daten der DTA nach den Aufrufen von
             Fsfirst/Fsnext in diese Struktur kopiert.

             Die XATTR-Struktur wurde zwar hinten an A_FileList
             angefÅgt, da jedoch Af_first und Af_next nun einen Zeiger
             auf eine A_FileList-Struktur oder NULL erwarten, um dort
             diese Daten einzutragen, mÅûten alte Module trotzdem neu
             compiliert werden, sorry!
         !end_itemize

 31.07.1998 Bibliothek (!link [ACS.LIB] [Pure-C-Bibliothek])

         !begin_itemize
           !item Bug behoben, durch den ab ca. dem 100-ten Eintrag eines
             Popups keine Subpopups mehr geîffnet wurden.

           !item Fehlverhalten von Ame_strpopup korrigiert. Es wurde evtl.
             ein falscher Teilstring geliefert. Beim Popup-String

                  "Test 1|Test 2|Test"

             wurde beim Klick auf "Test" der Teilstring

                  "Test 1|Test 2|Test"

             geliefert, also die erste Fundstelle des gewÑhlten
             Teilstrings. Jetzt wird - wie es sein soll - "Test"
             geliefert.
         !end_itemize

!end_node


!begin_node énderungen bis zur Version V2.3

Dies sind Korrekturen und Erweiterungen, die noch von (!link [Oliver Michalak] [Oliver Michalak])
gemacht wurden.

Hier eine Auflistung der Korrekturen innerhalb diverser Module von
ACSpro. Das Datum der énderung und das betroffene Modul werden
aufgelistet. Korrekturen an (!link [ACS.LIB] [Pure-C-Bibliothek]) oder (!link [ACSPLUS.LIB] [C-Erweiterungs-Bibliothek]) betreffen immer
auch die Pascal-Library.

!begin_itemize
   !item 20.8.96

      (!B)(!link [ACSPLUS.LIB] [C-Erweiterungs-Bibliothek]):(!b) Der EDITOR verkraftet nun eine andere Toolbar.
          Auûerdem kann sein Dateipfad per Nachricht ermittelt werden.
          Die Cursorposition oder Blockausmaûe werden angezeigt.

   !item 12.8.96

      (!B)purer Desktop:(!b) Unter SingleTOS wird nun ein blau-gemusterter
          Hintergrund angemeldet. Auûerdem beendet sich der Desktop
          nur noch, wenn 'appexit' auf TRUE steht.

   !item 6.8.96

      (!B)(!link [ACS.LIB] [Pure-C-Bibliothek]):(!b) Das letzte nicht-3-dimensionale Objekt (!B)A_cycle(!b) enthÑlt
          nun Paramater, die eine 3D-Optik ermîglichen, dazu wurde ein
          neuer A_cycle-Editor entworfen. Dementsprechend wird das
          Popup wenn mîglich nun ebenfalls 3-dimensional dargestellt.
          Dies hat unmittelbare Auswirkungen auf (!B)Ame_strpopup(!b) dessen
          sich die (!link [Click-Routine] [Click-Routine]) von (!B)Aus_cycle(!b) bedient. Weiterhin kann
          das (!link [Cycle] [Cycle])- Zeichen nun frei gewÑhlt werden.

      (!B)(!link [ACSPLUS.LIB] [C-Erweiterungs-Bibliothek]):(!b) Es wurde die neue Fensterklasse (!B)LIST-Window(!b)
          implementiert. Diese verwaltet dynamisch viele, frei
          definierbare Datentypen einer festen Grîûe. öber Callback-
          Routinen kann der Erzeuger die einzelnen Datenstrukturen
          editieren, einsortieren, per Drag&Drop bedienen lassen
          u.v.a.m. - inklusive von Blockfunktionen. Ungeîffnet erlaubt
          das Fenster Åber einige Nachrichten, Elemente aus der intern
          verwalteten Liste zu extrahieren, neue Elemente
          hinzuzufÅgen, etc.

          Davon profitiert zunÑchst der (!B)EDITOR(!b), der nun eine freie
          Funktionstastenbelegung anbietet. Auf bis zu 40 F-Tasten
          kann nun ein Text gelegt werden, der intuitiv per Listen-
          Fenster zu verarbeiten ist. Hinzu kamen weitere Feinheiten:

           !begin_xlist [manuelle Optionen-Sicherung]
           !item [Control-BS/-DEL]              beachtet nun die Worttrenner

           !item [SHIFT-BS/-DEL]                lîscht bis zum Zeilenanfang/-
                                        ende

           !item [freie Online-Hilfe]

           !item [manuelle Optionen-Sicherung]

           !item [Cursorpositionsanzeige]
           !end_xlist

          Gleichzeitig wurde der Fehler eliminiert, das in einem
          einzeiligen Text per (!B)Aed_selall(!b) nichts selektiert wurde.

          Aufgrund interner Umstellungen ist auch die (!B)A_editor(!b)-
          Nachricht (!link [AUO_EDPUTINFO] [AUO_EDPUTINFO]) weggefallen. DafÅr kann der (!B)EDITOR(!b)
          per AS_EDGETFNAME gefragt werden, wie der Dateiname fÅr den
          aktuellen Text lautet.

   !item 29.7.96

      (!B)ACS-Doku:(!b) Die Dokumentation wurde um die letzten Erweiterungen
          ergÑnzt -zusÑtzlich sind nun die Strukturen der ACS-PLUS-
          Library dokumentiert.

      (!B)(!link [ACS.LIB] [Pure-C-Bibliothek]):(!b) Bei Popups konnte die Maus busy bleiben - dieser Fehler
          ist behoben. ZusÑtzlich gibt es nun analog zu der Popup-
          Bedienung Nachrichten wÑhrend der MenÅbedienung.

   !item 22.7.96

      (!B)ACS-Builder:(!b) Ganz selten wurden Prototypen in der Referenzliste
          angelegt, wo kein ersichtlicher Bezug zu existierte (vor
          allem bei (!B)A_arrows(!b)).

   !item 21.7.96

      (!B)ACS-Builder:(!b) (!B)Tasten lernen(!b) funktionierte nicht sauber.
          ZusÑtzlich konnte man nicht korrekt Objekte verschieben.
          Schlieûlich konnten manche den Hinweistext im
          Informationsdialog der unregistrierten Version nicht lesen.

      (!B)(!link [ACS.LIB] [Pure-C-Bibliothek]):(!b) Fehlerhafte Auswertung der Indizees bei Sub-Popups mit
          Tastatursteuerung.

   !item 17.7.96

      (!B)(!link [ACS.LIB] [Pure-C-Bibliothek]):(!b) In (!B)Awi_dialog(!b) wurde nach dem Setzen von (!B)dia_abort(!b) nicht
          ein eventuell gedrÅckter Button-Index zurÅckgegeben. Zudem
          wurde ein Edit-Feld- Wechsel per Maus nicht von der
          Nachricht (!B)AS_FOCUSCHG(!b) begleitet.

   !item 11.7.96

      (!B)ACS-Builder:(!b) Es konnte kurzfristig kein Objekt kopiert (wohl
          aber dupliziert) werden.

          Im Icon-Editor hat der Farbpinsel bei mehr als 16 Farben die
          aktuelle Farbe nicht dargestellt. OBJECTs kînnen nun auch
          mit allen Komponenten dupliziert werden, so daû Teile des
          OBJECTs nicht mehr referenziert werden.

   !item 1.7.96

      (!B)ACSpro(!b) hat einen neuen (!B)Status(!b).

   !item 30.6.96

      (!B)(!link [ACS.LIB] [Pure-C-Bibliothek]):(!b) Ein Objektbaum mit defautable-Objekten, der kein
          Default enthÑlt, konnte bei Control-Tab ACS zum abstÅrzen
          bringen.

   !item 21.6.96

      (!B)(!link [ACS.LIB] [Pure-C-Bibliothek]):(!b) In einigen (N)VDI-Versionen erscheint die Textselektion
          im (!B)A_boxed(!b)- Objekt zumindest bei der Verwendung von 256
          Farben als schwarzes Rechteck, ohne daû der darunter
          liegende Text invers dargestellt wird. Trotz einiger
          Versuche, diesen Fehler zu umschiffen, habe ich ad hoc keine
          Lîsung gefunden.

   !item 13.6.96

      (!B)(!link [ACS.LIB] [Pure-C-Bibliothek]):(!b) Es konnte kurzfristig per MultiDateiAuswahl nicht mehr
          als eine Datei eingelesen werden.

          Ist (!B)AB_LAZYEVAL(!b) gesetzt, wird in (!B)Ash_prog(!b) nicht mehr der
          Standardpfad selber gesetzt.

          Auûerdem konnte es in (!B)Awi_alert(!b) vorkommen, daû die Maus
          dauerhaft als Biene angezeigt wurde.

   !item 3.6.96

      (!B)ACS-Builder:(!b) Die Minimal- und Maximalrechtecke beim VerÑndern
          der Objektgrîûe konnten insbesondere beim Popup-Editor mit
          Sub-Popups merkwÅrdige Ausmaûe annehmen. öbrigens: Mit der
          rechten Maustaste kann man das "Aufziehen" abbrechen.

      (!B)purer Desktop:(!b) Wenn sich die Anordnung der Fenstericons im
          Module-Fenster Ñnderte, wurden die Stati der Icons nicht
          mitverschoben, woduch plîtzlich Icons nicht mehr als Ghost
          erschienen oder umgekehrt.

   !item 30.5.96

      (!B)ACS-Builder:(!b) FÅr Pascal wurde eine fehlerhafte OberflÑche
          exportiert (kein (!B)ob_len(!b)).

   !item 28.5.96

      (!B)ACS-Builder:(!b) Eine Tastendruck im Icon-Editor fÅhrte zum Absturz.

   !item 26.5.96

      (!B)ACS-Builder:(!b) Einstellungen um SchlÅsselregistrierung erweitert.

   !item 22.5.96

      (!B)purer Desktop:(!b) Ein Icon-Fenster konnte nicht per MenÅpunkt
          "ôffnen" geîffnet werden.

   !item 16.5.96

      (!B)(!link [ACS.LIB] [Pure-C-Bibliothek]):(!b) Ein Fehler im (!link [MagiCMac] [Mag!X])-OS lief darauf hinaus, daû ein
          ikonifiziertes Fenster ohne Anmeldung wieder normal
          dargestellt wurde. ACS versucht dies nun abzufangen.

          Bei (!B)A_boxed(!b) wurde der Cursor nicht immer in den sichtbaren
          Bereich verschoben.

   !item 13.5.96

      (!B)(!link [ACS.LIB] [Pure-C-Bibliothek]):(!b) (!B)A_boxed(!b)-(!link [Click-Routine] [Click-Routine]) fÅr Fenster mit Slidern
          korrigiert.

          Es versteht nun diverse Datum- und Zeitformate.

          Peinliche Debug-Printf-Ausgaben unterbunden.

   !item 9.5.96

      (!B)ACS.PAS:(!b) Korrekturen in einigen Definitionen.

   !item 8.5.96

      (!B)(!link [ACS.LIB] [Pure-C-Bibliothek]):(!b) Farbicons wurden unter MagiC nicht automatisch farbig
          dargestellt, entweder man setzt (!B)AB_NOTRANSICON(!b) manuell oder
          nimmt die neue Library.
!end_itemize

!end_node


!end_node
